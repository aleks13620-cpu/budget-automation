# 📋 ПЛАН РЕАЛИЗАЦИИ СИСТЕМЫ АВТОМАТИЗАЦИИ РАСЧЁТОВ

**Проект:** Система автоматизации обработки счетов для инженерных сетей  
**Инструменты:** Claude Code, VS Code/Cursor  
**База данных:** SQLite  
**Стратегия:** Постепенное обучение (начало с 1 проекта)

---

## 📊 ОБЩАЯ СТРУКТУРА

**Всего этапов:** 9  
**Общее время:** 67-78 часов  
**Максимум на этап:** 8 часов

---

## 🔷 ЭТАП 1: ИНИЦИАЛИЗАЦИЯ ПРОЕКТА И СТРУКТУРА БД

**Время:** 6-8 часов  
**Инструмент:** Claude Code + VS Code

### Задачи:

1. **Создание структуры проекта** (1 час)
   - Папки: `/backend`, `/frontend`, `/data`, `/database`
   - Инициализация Git-репозитория
   - Создание `.gitignore`, `README.md`
   - Настройка переменных окружения (`.env`)

2. **Установка зависимостей** (1 час)
   - Python: FastAPI, SQLAlchemy, pdfplumber, openpyxl
   - Node.js: React (Vite), Axios
   - SQLite драйвер

3. **Создание схемы базы данных** (3-4 часа)
   - Таблицы:
     - `projects` (проекты)
     - `specification_items` (позиции спецификации)
     - `suppliers` (поставщики)
     - `invoices` (счета)
     - `invoice_items` (позиции счетов)
     - `matching_rules` (правила сопоставления)
     - `matched_items` (результаты сопоставления)
     - `supplier_parser_configs` (настройки парсеров)
   - Создание миграций
   - Инициализация пустой БД

4. **Базовое API** (1-2 часа)
   - Настройка FastAPI
   - Endpoint для проверки: `GET /api/health`
   - Подключение к БД
   - Тестовый запрос

### Результат:
✅ Структура проекта готова  
✅ База данных создана  
✅ API запущено и отвечает  
✅ Git-репозиторий настроен

---

## 🔷 ЭТАП 2: ЗАГРУЗКА И ИМПОРТ СПЕЦИФИКАЦИЙ

**Время:** 6-8 часов  
**Инструмент:** Claude Code

### Задачи:

1. **Парсер Excel-спецификаций** (3-4 часа)
   - Функция чтения `.xlsx` файлов
   - Определение начала таблицы (пропуск заголовков)
   - Извлечение колонок:
     - Номер позиции
     - Наименование
     - Характеристики
     - Код оборудования
     - Производитель
     - Единица измерения
     - Количество
   - Валидация данных

2. **API для загрузки спецификаций** (2-3 часа)
   - `POST /api/projects` — создание проекта
   - `POST /api/projects/{id}/specification` — загрузка файла
   - Сохранение позиций в таблицу `specification_items`
   - Обработка ошибок

3. **Автоопределение разделов** (1 час)
   - Базовая логика по ключевым словам:
     - "кабель", "провод" → Электрика
     - "труба", "задвижка" → ВК
     - "вентилятор", "диффузор" → Вентиляция
     - "радиатор", "котёл" → Отопление

### Результат:
✅ Можно загрузить Excel-спецификацию  
✅ Позиции сохраняются в БД  
✅ Разделы определяются автоматически

---

## 🔷 ЭТАП 3: УНИВЕРСАЛЬНЫЙ ПАРСЕР PDF (БАЗОВЫЙ)

**Время:** 8 часов  
**Инструмент:** Claude Code

### Задачи:

1. **Чтение PDF и извлечение таблиц** (3 часа)
   - Использование `pdfplumber`
   - Извлечение всех таблиц со страницы
   - Поиск начала таблицы по ключевым словам

2. **Автоопределение колонок** (3 часа)
   - Анализ заголовка таблицы
   - Поиск колонок:
     - Артикул: "артикул", "article", "код"
     - Название: "наименование", "товар", "название"
     - Количество: "количество", "кол-во", "qty"
     - Цена: "цена", "price"
     - Сумма: "сумма", "total"
     - Ед.изм: "ед.", "unit"
   - Создание маппинга колонок

3. **Извлечение данных из строк** (2 часа)
   - Парсинг каждой строки таблицы
   - Очистка данных (пробелы, валюты)
   - Преобразование цен: "1 000,50 руб." → 1000.50
   - Сохранение в структуру данных

### Результат:
✅ Парсер читает простые PDF-счета  
✅ Автоматически определяет колонки  
✅ Извлекает позиции с ценами

---

## 🔷 ЭТАП 4: ПАРСЕР EXCEL-СЧЕТОВ

**Время:** 5-6 часов  
**Инструмент:** Claude Code

### Задачи:

1. **Чтение Excel-счетов** (2 часа)
   - Использование `openpyxl`
   - Поиск начала таблицы (пропуск реквизитов)
   - Определение последней строки (итоги)

2. **Автоопределение колонок** (2 часа)
   - Логика как в PDF-парсере
   - Обработка merged cells (объединённых ячеек)

3. **API для загрузки счетов** (1-2 часа)
   - `POST /api/invoices/upload` — загрузка файла
   - Определение типа (PDF/Excel)
   - Вызов соответствующего парсера
   - Сохранение в `invoices` и `invoice_items`

### Результат:
✅ Парсер читает Excel-счета  
✅ Можно загружать счета через API  
✅ Позиции сохраняются в БД

---

## 🔷 ЭТАП 5: ИНТЕРФЕЙС НАСТРОЙКИ ПАРСЕРА

**Время:** 8 часов  
**Инструмент:** VS Code/Cursor (React)

### Задачи:

1. **API предпросмотра таблицы** (2 часа)
   - `GET /api/invoices/{id}/preview` — первые 10 строк
   - Возврат сырой таблицы (массив массивов)

2. **React-компонент настройки** (4 часа)
   - Отображение таблицы из счёта
   - Dropdown для каждой колонки:
     - "Артикул"
     - "Наименование"
     - "Количество"
     - "Цена"
     - "Сумма"
     - "Пропустить"
   - Сохранение маппинга

3. **Сохранение конфигурации** (2 часа)
   - `POST /api/suppliers/{id}/parser-config`
   - Сохранение в `supplier_parser_configs`
   - Применение конфигурации при следующем парсинге

### Результат:
✅ Оператор может настроить формат счёта  
✅ Настройка сохраняется для поставщика  
✅ Следующие счета парсятся автоматически

---

## 🔷 ЭТАП 6: АЛГОРИТМ СОПОСТАВЛЕНИЯ

**Время:** 8 часов  
**Инструмент:** Claude Code

### Задачи:

1. **Функция нормализации текста** (1 час)
   - Приведение к нижнему регистру
   - Удаление лишних пробелов
   - Замена символов: Ø→d, х→x, ё→е

2. **Сопоставление по артикулу** (1 час)
   - Точное совпадение артикулов
   - Уверенность: 95%

3. **Нечёткое сопоставление по названию** (3 часа)
   - Алгоритм: SequenceMatcher (Левенштейн)
   - Порог: 70% схожести
   - Расчёт уверенности: 70-95%

4. **Проверка правил обучения** (2 часа)
   - Поиск в `matching_rules`
   - Если правило есть → применить
   - Уверенность из правила

5. **API для сопоставления** (1 час)
   - `POST /api/projects/{id}/match` — запуск
   - Для каждой позиции спецификации → топ-3 варианта
   - Сохранение в `matched_items`

### Результат:
✅ Система находит совпадения  
✅ Топ-3 варианта для каждой позиции  
✅ Использует правила обучения

---

## 🔷 ЭТАП 7: UI ПОДТВЕРЖДЕНИЯ СОПОСТАВЛЕНИЙ

**Время:** 8 часов  
**Инструмент:** VS Code/Cursor (React)

### Задачи:

1. **Страница списка позиций** (2 часа)
   - Таблица всех позиций спецификации
   - Статусы: ✅ подтверждено, ⚠️ требует проверки, ❌ не найдено
   - Фильтры по статусу

2. **Карточка сопоставления** (4 часа)
   - Отображение позиции спецификации
   - Топ-3 варианта из счетов
   - Для каждого:
     - % совпадения (цветовая индикация)
     - Поставщик
     - Цена и сумма
     - Кнопки: "Подтвердить", "Подтвердить как аналог", "Отклонить"

3. **Сохранение решений** (2 часа)
   - `POST /api/matches/{id}/confirm`
   - Обновление `matched_items`
   - Создание правила в `matching_rules` (если аналог)
   - Автопереход к следующей позиции

### Результат:
✅ Оператор видит все сопоставления  
✅ Может подтверждать/отклонять  
✅ Система запоминает решения

---

## 🔷 ЭТАП 8: ГЕНЕРАЦИЯ ИТОГОВОЙ СПЕЦИФИКАЦИИ

**Время:** 6-8 часов  
**Инструмент:** Claude Code

### Задачи:

1. **Выбор минимальной цены** (2 часа)
   - Для позиций с несколькими вариантами
   - Автовыбор минимальной (с подсветкой)
   - Возможность вручную выбрать другую

2. **Расчёт итогов** (2 часа)
   - Сумма по позициям
   - Группировка по разделам
   - Итоги по разделам
   - Общий итог

3. **Экспорт в Excel** (2-3 часа)
   - Формирование структуры:
     - Заголовки
     - Разделы (жирным шрифтом)
     - Позиции с ценами
     - Итоги по разделам
     - Общий итог
   - API: `GET /api/projects/{id}/export`

4. **Сравнение с эталоном** (1 час)
   - Загрузка эталонного файла
   - Сравнение итоговых сумм
   - Вывод разницы (абс. и %)

### Результат:
✅ Система генерирует итоговую спецификацию  
✅ Экспорт в Excel  
✅ Можно сравнить с эталоном

---

## 🔷 ЭТАП 9: ПОЛИРОВКА И ТЕСТИРОВАНИЕ

**Время:** 6-8 часов  
**Инструмент:** VS Code/Cursor

### Задачи:

1. **UI/UX полировка** (2 часа)
   - Единый стиль
   - Индикаторы загрузки
   - Сообщения об ошибках
   - Подсказки для оператора

2. **Обработка ошибок** (2 часа)
   - Try-catch блоки
   - Логирование ошибок
   - Понятные сообщения пользователю

3. **Тестирование на Проекте №1** (2-3 часа)
   - Загрузка всех данных
   - Полный цикл: спецификация → счета → сопоставление → экспорт
   - Проверка результата
   - Фиксация багов

4. **Документация** (1 час)
   - README с инструкциями запуска
   - Краткая инструкция для оператора
   - Комментарии в коде

### Результат:
✅ Система готова к использованию  
✅ Протестирована на реальных данных  
✅ Документация написана

---

## 📊 ИТОГОВАЯ ТАБЛИЦА

| Этап | Название | Часы | Инструмент |
|------|----------|------|------------|
| 1 | Инициализация и БД | 6-8 | Claude Code + VS Code |
| 2 | Импорт спецификаций | 6-8 | Claude Code |
| 3 | Парсер PDF (базовый) | 8 | Claude Code |
| 4 | Парсер Excel-счетов | 5-6 | Claude Code |
| 5 | UI настройки парсера | 8 | VS Code/Cursor |
| 6 | Алгоритм сопоставления | 8 | Claude Code |
| 7 | UI подтверждения | 8 | VS Code/Cursor |
| 8 | Генерация итоговой спецификации | 6-8 | Claude Code |
| 9 | Полировка и тестирование | 6-8 | VS Code/Cursor |
| **ИТОГО** | | **67-78 часов** | |

---

## 🎯 ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫПОЛНЕНИЯ

**Рекомендуемый порядок:**

1. Этапы 1-2 → **Основа** (база данных + импорт спецификаций)
2. Этапы 3-5 → **Парсинг** (счета от поставщиков)
3. Этапы 6-7 → **Сопоставление** (ядро системы)
4. Этап 8 → **Результат** (генерация спецификации)
5. Этап 9 → **Доведение до ума**

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ КАЖДОГО ЭТАПА

**Этап считается завершённым, если:**

- ✅ Код написан и протестирован
- ✅ API endpoints работают (если есть)
- ✅ UI отображается корректно (если есть)
- ✅ Нет критических ошибок
- ✅ Изменения закоммичены в Git

---

## 🔄 ТОЧКИ ПРОВЕРКИ

**После Этапа 2:** Можно загрузить спецификацию, позиции в БД  
**После Этапа 4:** Можно загрузить счета, позиции в БД  
**После Этапа 6:** Система находит совпадения (даже неточные)  
**После Этапа 8:** Можно сгенерировать итоговую спецификацию  
**После Этапа 9:** Система готова к передаче клиенту

---

## 📝 ПРИМЕЧАНИЯ

### База данных SQLite
- Файл БД: `/database/budget_automation.db`
- Легко копируется и переносится
- Не требует отдельного сервера
- Подходит для 1-10 одновременных пользователей

### Стратегия обучения
- **Первый проект:** Точность 60-70%, оператор тратит 3-4 часа
- **Пятый проект:** Точность 75-80%, оператор тратит 1 час
- **Десятый проект:** Точность 80-85%, оператор тратит 30 минут

### Хранение базы знаний
- Все правила сопоставления в таблице `matching_rules`
- Настройки парсеров в `supplier_parser_configs`
- База знаний растёт автоматически при работе оператора
- Можно делать резервные копии файла БД

---

**Документ подготовлен:** 06.02.2026  
**Версия:** 1.0
