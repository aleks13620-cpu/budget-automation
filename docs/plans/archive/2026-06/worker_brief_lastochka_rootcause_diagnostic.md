# Воркер-бриф: ДИАГНОСТИКА Ласточки — раздельный root-cause (парсинг vs покрытие vs матчинг)

> Открой новый чат Claude Code в `C:\Users\home\vscode101\budget-automation` и напиши:
> «выполни docs/plans/active/worker_brief_lastochka_rootcause_diagnostic.md». Самодостаточно.
> ⚙️ Нетривиально (классификация + ре-парс PDF). Базовая модель не тянет — владелец запустит сильнее.

## Роль
Воркер. Ветка **`feat/canonical-kb`** (есть готовый worktree `C:\Users\home\vscode101\ckb2` с `npm ci`; `git -C ../ckb2 log -1`). **ОФЛАЙН-диагностика: прод ТОЛЬКО read-only GET, без push/деплоя.** Итог — `worker_brief_lastochka_rootcause_diagnostic_result.md`. **В ПАМЯТЬ проекта НЕ писать** (память ведёт оркестратор) — только result.md.
⚠️ Общий worktree может быть занят — `git rev-parse` перед коммитом.

## Где это в плане
Развилка A North-Star плана: на реальном проекте оператора **Ласточка (пр.11, ОВ)** = **26.7% AI-ON / 3.3% durable** на 150 confirmed-парах (kill-зона, <60%). Прежде чем заключать «нужны данные заказчика», надо РАЗДЕЛИТЬ корень: это наш ресурс (парсер) или ресурс заказчика (ОВ-данные) или матчер. Цель — дать владельцу честный диагноз для решения демо-стратегии.

## Ключевая гипотеза (проверить, не утверждать заранее)
Прод pr.11 спека, скорее всего, **разобрана СТАРЫМ парсером — сироты/развал иерархии (#475)**: parent_item_id ~10/328, дочерние строки без родителя → `full_name` = фрагмент без контекста («C11-300-500», «То же»). Новый иерархи-парсер задеплоен (`0c41f06`), но **forward-only — прод НЕ ре-парсился** (re-upload pr.11 ЗАПРЕЩЁН: CASCADE сотрёт 86 ручных связок). → бенчмарк, вероятно, кормил матчер мусором (no-corrupt-through: мусор на входе → мусор на выходе, это НЕ вина матчера).

## Метод (по порядку; п.1–3 — основное, п.4 — подтверждение)
1. **Снять 150 пар** (spec `full_name`, подтверждённый аналог, пул счетов pr.11) — read-only GET, переиспользуй `scripts/verity-lastochka.mjs`.
2. **Классифицировать КАЖДУЮ spec-строку: PARSE_OK vs PARSE_BROKEN.** Эвристика (документируй её): PARSE_BROKEN = orphan-фрагмент / голый код-типоразмер / «То же» / нет типа-головы (переиспользуй `looksBareFragment`/orphan-логику из `backend/src/services/gigachatSpecParseQuality.ts`). **ОБЯЗАТЕЛЬНО глазами перепроверь выборку 15–20 строк** (evidence-before-claims — эвристика без проверки = шум).
3. **Классифицировать ПОКРЫТИЕ:** bridgeable ли пара через каталог-из-истории (spec и аналог делят canon-паспорт) — переиспользуй bridgeable-диагностику из history-exam (`getCanonicalByAttributes`/`getCanonicalByAlias`).
4. **Пере-замерить верность@1 (AI-ON и durable) на ПОДМНОЖЕСТВАХ:** ВСЕ 150 / только PARSE_OK / PARSE_OK ∩ bridgeable. **Дельта = ответ:** если на PARSE_OK резко выше → парсинг главный тормоз (наш ресурс); если и на чистых низко → покрытие/домен (данные заказчика).
5. **(подтверждение, если успеваешь) Ре-парс PDF Ласточки ОФЛАЙН новым парсером** (`C:\Users\home\Downloads\…5-ПР_21 – ОВ (1)-45-75.pdf`, через `parseSpecFromPdf`, Gemini-фолбэк) во ВРЕМЕННУЮ БД → сколько prod-сиротских позиций новый парсер чинит (даёт родителя/контекст). **НИКОГДА не писать в прод pr.11.**

## Вернуть в result.md
- **Разбивка 150** по корню: N PARSE_BROKEN / N coverage-gap / N match-fail (+ пересечения). С процентами.
- **Верность@1 (AI-ON + durable):** ВСЕ 150 vs PARSE_OK vs PARSE_OK∩bridgeable (таблица).
- **Оценка ре-парса:** сколько сирот чинит новый парсер (recoverable нашим ресурсом).
- **ВЕРДИКТ:** доминирующая причина + чем закрывается (наш парсер / ОВ-данные заказчика / работа над матчером). Если парсинг доминирует — Ласточка чинится БЕЗ заказчика (через аккуратное восстановление спеки, НЕ re-upload).
- Честные оговорки (150 confirmed — прокси истины; пул; выборка).

## Ограничения / правила проекта
- **Проект 11 в проде НЕ трогать:** только read-only GET; ре-парс — в ТЕМП-БД/офлайн; **никаких re-upload/reparse на проде** (CASCADE сотрёт 86 связок, `schema.ts` ON DELETE CASCADE).
- `feedback_no_corrupt_through` (мы как раз меряем вклад битого парса), `feedback_evidence_before_claims` (эвристика + ручная сверка выборки; цифры — прогоном), `feedback_no_hardcode`.
- Без push/деплоя. ASCII-коммит, именованные файлы (диаг-скрипт + result). matcher.ts НЕ трогать. result.md, НЕ память.
- ОФЛАЙН eval → гейт = честность (классификация обоснована+сверена, подмножества корректны, прод не записан). ≤4–6 ч; перебор → СТОП, верни частичное (минимум п.1–4).

## Гейт «Готово»
Разбивка 150 по корню + таблица верности по подмножествам + вердикт (парсинг/покрытие/матчинг) + оценка ре-парса. Оркестратор сверяет честность и классифицирует по развилке A (наш ресурс vs данные заказчика).
