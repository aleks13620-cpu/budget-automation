# Стартовый промпт — Оркестратор: North-Star рост качества + рычаг вариант-семей (точка остановки 2026-06-08)

> Скопируй в новый чат Claude Code, открытый в `C:\Users\home\vscode101\budget-automation`.
> Ты — **оркестратор** (агент «Арта»): код САМ не пишешь, ведёшь процесс; код-фазы = воркер-чаты по `worker_brief_*.md`; гейтишь как **diff/факт** (не по рассказу); контекст держишь чистым (детали — воркерам); **память ведёшь ТЫ** (воркеры пишут только `*_result.md`).

## 0. Прочитай ПЕРВЫМ (память — живая нить)
- **`project_masterplan_decisions_2026-06-05.md`** (auto-memory) — ГЛАВНЫЙ журнал, читать целиком; критичны записи **(1)–(10) от 2026-06-08**.
- `docs/plans/active/master_plan_phased_2026-06-05.md` — секции **«ПЛАН North-Star»** и «ФАЗА-2 КАТАЛОГА» (метрика, фазы A→D, развилки, ВАУ-планка).
- Рефлексии 2026-06-08: `reflection_2026-06-08_canonical_kb_history_transfer` (кросс-проектный перенос подтверждён), `reflection_2026-06-08_lastochka_clean_representation_lever` (рычаг вариант-семей). 2026-06-07: `..._parser_ai_hierarchy`, `..._canonical_kb_phase1`, `..._canonical_kb_phase2_matcher`. Фон: `reflection_2026-06-04_operator_trust_and_strategy_risk`.
- Правила: `feedback_no_corrupt_through`, `feedback_evidence_before_claims`, `feedback_no_action_without_confirmation`, `feedback_arta_autonomy_and_roles`, `feedback_shared_worktree_branch_hazard`, `feedback_no_hardcode`, `feedback_build_before_push`, `feedback_commit_named_files`.

## 1. Стратегическое видение (на данных этой сессии — доказано прогонами)
**Главная ставка ПОДТВЕРЖДЕНА:** уход от «арендованного у ИИ» матчинга (89%, но платно/флаки) к СВОЕМУ durable-знанию. Доказаны ДВА own-ресурс рычага:
1. **Каталог-паспортов (кросс-проектная память):** из ИСТОРИИ на held-out Сокольих = **46.4% durable @1** (> идеализир. пул 42.9%). Перенос работает; узкое место = ПОКРЫТИЕ паспортов, не качество (bridgeable→100% top-3).
2. **Чистое представление вариант-семей:** на РЕАЛЬНОМ проекте оператора (Ласточка ОВ) durable **1%→74.3%** на классе жалоб / **2.7%→52%** overall, БЕЗ ИИ и БЕЗ данных заказчика — убрав ШУМ из ключа матча (вариант-префикс/«Q=…Вт»). НЕ parent-linking (он шум не убрал, 0%).

**Мастер-переменные:** ПОКРЫТИЕ ДОМЕНА (история) + ЧИСТОТА КЛЮЧА МАТЧА. Где есть оба — сильно (ВК Сокольи 89% AI-ON). Слабость ОВ (Ласточка) — **во многом НАШ ресурс (представление), а не только данные заказчика.**

**North-Star метрика (растёт = качество растёт):** **верность матчинга@1** против эталона (НЕ «покрытие/сматчено»). Две проекции: **AI-ON** (операторская, **ВАУ-планка ≥80%**, <60%=kill-зона) + **durable@1** (стратегич. капитал, ИИ-независимый). Мерим офлайн-бенчмарком ВНЕ зоны оператора.

**Доверие оператора (риск №1):** скептик (сомнение в согласованности между поставщиками); **первое впечатление одноразовое → оператору НЕ показываем, пока offline-замер не докажет проект ≥~80% AI-ON.** Рычаг чистого представления прямо адресует жалобы #433/#326.

## 2. Точка git
- **`main = 0c41f06`** — задеплоено: парсер ИИ-иерархии (forward) + маркировка-дискриминатор. **confirmed P6=268 / P11=150 целы** (live-проверено).
- **`feat/canonical-kb`** (в worktree **`C:\Users\home\vscode101\ckb2`**, `npm ci` сделан):
  - **ДЕПЛОЕБЕЛЬНЫЕ канон-коммиты (5-move PASS, НЕ задеплоены):** `59daecb` (схема KB) + `d1f3439` (фикс alias-индекса) + `96ee438` (интеграция KB в matcher; **инертна при пустом каталоге** → код-деплой безопасен).
  - **EVAL-коммиты — НА MAIN НЕ НЕСТИ:** `b839a0f` (history-exam), `d394e90` (multi-project бенчмарк), `d68ac18` (Ласточка root-cause), `d847864` (Ласточка ре-парс суррогат). Это verity-харнессы/результаты.
- ⚠️ **Общий worktree + ckb2:** ВСЕГДА `git rev-parse --abbrev-ref HEAD` перед коммитом. **Деплой канона = ЧИСТЫЙ cherry-pick ТОЛЬКО `59daecb+d1f3439+96ee438`** на свежую ветку от `origin/main` (verity/eval-обвес не тащить — как делали с парсером).
- `feat/parser-ai-hierarchy` @ `9c0348d` — заменён чистым cherry-pick; можно удалить (CARRY).

## 3. Открытая работа (приоритеты + развилки/метрики)
1. **🔴 СРОЧНО (ops, по живому): пополнить OpenRouter.** Кредиты исчерпаны (HTTP **402**). Бьёт по проду: LLM-матчинг (если `ENABLE_OPENROUTER_LLM_MATCHING=true`), Gemini-фолбэк парсера спек, OCR счетов. **Возможная причина текущей деградации матчинга у оператора.** Проверить баланс + что включено в проде. Ключ/модель `google/gemini-2.5-flash` исправны — дело в балансе.
2. **🟢 ГЛАВНЫЙ own-ресурс ход — deploy-кандидат «чистое представление вариант-семей».** Реализовать в парсере/ингесте **ОБОБЩЁННО** (не хардкод-anchor «Радиатор|Конвектор»): `full_name` (ключ матча) = чистое имя продукта; вариант (исполнение/подключение/Q/мощность) → `characteristics`. **matcher.ts НЕ трогать** (уже `max(sim(name),sim(full_name))` + тир characteristics). Замер на РЕАЛЬНОМ парсе + multi-project бенчмарке (НЕ суррогате — суррогат дал UPPER BOUND 74.3%). **Гейт: 5-move + тривайр 268/150** (меняет вход для ВСЕХ проектов). Существующая пр.11: re-split `full_name` БЕЗ re-upload (CASCADE/86) ИЛИ нормализация на матч-тайме — дизайн воркера. **Метрика: durable@1 + AI-ON@1 «до/после».** Это путь к WOW на проекте оператора нашими силами.
3. **🟡 Деплой каталога** (`59daecb+d1f3439+96ee438`): чистый cherry-pick → ре-бутстрап в проде с **ПРОД-синонимами** (C1 — деплой-блокер бутстрапа, не кода) → прод-замер. Код инертен при пустом каталоге (безопасен), ценность — после бутстрапа.
4. **Дашборд North-Star (Фаза D):** тонкая обёртка над бенчмарк-метрикой (для владельца, вне зоны оператора). Движок-метрика = `verity-historical.mjs` / `verity-lastochka.mjs` / surrogate в ckb2.
5. **CARRY:** C2 клапан/кран = **0 эффекта (мёртв, не тратить)**; Tepla-конвекторы покрытие; больше истории = выше bridge; фронт-лейбл `catalog_kb`; `/api/version` (маркер свежести); `gigachatLimiter` на spec-маршруты; удалить ветку `feat/parser-ai-hierarchy`.

**Развилки (мастер-план «ПЛАН North-Star»):** AI-ON@1 ≥80%→ВАУ-демо / 60–80→чинить узкое место / <60→корневой разбор. Дельта рычага плато (+3–5%)→текущие данные исчерпаны, нужны новые. Перенос на разных проектах (split)→ставка крепка.

## 4. Правила (во всех фазах)
- Оркестратор код не пишет; фазы — воркер-чаты по `worker_brief_*.md`; гейт = diff/факт; воркеры → `result.md`, **НЕ память**; память ведёт оркестратор.
- Перед push в `main`: `npm run build` + **5-move `pre-deploy-check` PASS**. **Тривайр confirmed 268/150 НЕ ронять.** **Ничего в прод без «ок» владельца.**
- **Проект 11 НЕ перезаливать** (CASCADE сотрёт 86 ручных связок). `no-corrupt-through`, `no-hardcode`, `evidence-before-claims`. ASCII-коммиты, ТОЛЬКО именованные файлы. **Чистый cherry-pick на деплой** (verity/eval-обвес на main не нести).
- Прогоны — воркеры в отдельных чатах (модель эскалировать при нужде: задачи нетривиальны); оркестратор не жжёт контекст.
- Параллельно (депри­оритизировано): 14 неразобранных операторских замечаний — оператор реакции не ждёт, доверие вернёт работающая система; рычаг чистого представления адресует радиаторный под-класс (#433/#326).

## 5. Первый ход нового чата
Подтверди роль + прочитай память (раздел 0). Затем с владельцем зафиксируй приоритет: **(1) ops OpenRouter (срочно, по живому) → (2) бриф «чистое представление вариант-семей» (главный own-ресурс ход к WAU)**, далее (3) деплой каталога. Совет оркестратора: 1 сейчас, 2 — основной фокус. Роль — оркестратор, фокус сверху; цифры — прогоном воркеров, ты гейтишь честность (утечка/воспроизводимость/тривайр) и ведёшь память.
