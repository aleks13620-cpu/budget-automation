# Результат: матчер — дискриминатор маркировки/типоразмера

> Воркер-итог по `docs/plans/active/worker_brief_marking_discriminator.md`.
> **Статус: ГОТОВО. STOP/split НЕ потребовался. Миграция БД НЕ нужна** (токенов из имён хватило на все 5 классов признаков). Ветка, без push/деплоя; прод/БД/проект 11 не тронуты.

## Ветка
`feat/matcher-marking-discriminator` (от `main`). Коммит локальный, НЕ запушен.

## Изменённые файлы
| Файл | Что |
|---|---|
| `backend/src/services/matcher.ts` | Ядро: распознавание признаков + дискриминатор + штраф |
| `backend/package.json` | + npm-скрипт `test:matcher` |
| `scripts/test-matcher-marking-discriminator.mjs` | **НОВЫЙ** red-first тест-каркас (по образцу `test-spec-parent-child.mjs`) |
| `docs/plans/active/worker_brief_marking_discriminator_result.md` | этот итог |

## Что сделано (по скоупу брифа)

### 1. Расширено распознавание токенов признаков
- **Кириллический «Дн»/«дп» → DN.** Новое правило в `normalizeEngineeringTokens` (требует цифры после, чтобы «дно»/«переходной» не трогать).
- **`extractDnValue` стал DB-free** (через новый `normalizeStructuralTokens` = `removeGostBrackets` + `normalizeEngineeringTokens`) и теперь терпит:
  - суффикс размера, приклеенный к цифрам: `dn57x3.5` → 57 (раньше `\b` после цифр ломался об `x` → возвращал `null` даже для латиницы);
  - сепараторы: `DN-50`, `DN. 50` → 50 (как раньше делал полный пайплайн через strip пунктуации).
- **Новая чистая функция `extractMarkingFeatures(text)`** (экспортирована, DB-free, юнит-тестируема) извлекает:
  - `dn` — диаметр (DN/Ду/Дн/дп/ø);
  - `cross` — сечение «AxB», напр. `57x3.5`;
  - `config` — многоместный конфиг «NN-NN-NN» (тройник переходной 16-20-16 vs равнопроходный 16-16-16);
  - `marks` — короткие латинские буквенные маркировки (CV/CVL/C); бренды Title-case и коды, приклеенные к цифрам (DN15/PN16), не ловятся;
  - `sizes` — «голые» целочисленные размеры (фиксаторы 16/20/25), исключая числа, уже учтённые в dn/cross/config; **номера ГОСТ/ТУ/DIN вырезаются** (`STANDARD_REF_RE`), чтобы не считались размерами.

### 2. Штраф за рассогласование — зеркалит и обобщает DN-штраф
`getDnScore` → переименована в **`getStructuralScore`** и обобщена на все измерения. Логика:
- **CONFLICT** (оба несут признак, но РАЗНЫЕ) → `-1` (доминирует — «не тот вариант»);
- **AGREEMENT** (хоть одно измерение совпало, конфликтов нет) → `+1`;
- иначе односторонний пропуск DN (спека DN-типизирована, в счёте DN нет): `-1` **только если** счёт вообще не упоминает это число (иначе голый размер = тот же диаметр → `0`) — сохраняет ловлю «труба vs теплоизоляция»;
- иначе `0`.

**Штраф не дублировался отдельной строкой — он влит в существующий `dnScore`.** Поле `dnScore` — это и есть структурный дискриминатор; имя сохранено для совместимости с потребителями ранжирования. Поэтому ВСЕ существующие места уже работают без правок:
- `matcher.ts` штраф `dnScore===1 → +0.1`, `dnScore===-1 → −0.18` (matcher.ts:~592-593) — «жёсткий штраф»;
- `matcher.ts` первичный ключ сортировки кандидатов (`b.dnScore - a.dnScore`);
- `routes/matching.ts` выбор авто-#1 (`bestCandidateBySpec`, :487-492);
- dev-тулзы `replay-matching.ts`, `bench-matching.mjs`.

**Почему влил в `dnScore`, а не добавил параллельный `markingScore`:** `dnScore` — ПЕРВИЧНЫЙ ключ ранжирования и в matcher-сортировке, и в выборе авто-#1 в роуте. Отдельный score, не добавленный в этот ключ, позволил бы кандидату с верным DN, но НЕВЕРНОЙ маркировкой обойти верный кандидат по первичному ключу. Влитие гарантирует демоушн «не того варианта» во ВСЕХ путях ранжирования. Бонус: **миграция БД не нужна** — `dnScore`/`quantityScore` не персистятся (в `matched_items` пишется `confidence`, не они).

## Red-first — соблюдён
Сначала написан каркас, прогнан против скаффолда (rename + DB-free `extractDnValue`, БЕЗ маркинг-логики): **15 падали** (A2-A4 кириллица, B/C/D/E новые фичи, G извлечение), **5 проходили** (A1 латиница + F1-F4 DN-гарды — доказывает сохранение DN-поведения). Затем реализован фикс → **25/25 зелёные.**

Прогон: `npm run test:matcher` (из `backend/`) или `node scripts/test-matcher-marking-discriminator.mjs`.

| Группа | Проверяет | Итог |
|---|---|---|
| A1-A5 | DN: латиница, кириллица Дн, x-суффикс, сепаратор `DN-50` | PASS |
| B1 | труба «Дн57х3,5» vs изоляция «ThermaECO P-60» → `-1` (пример с прода) | PASS |
| C1/C2 | тройник 16-20-16 ≠ 16-16-16; 16-16-16 = 16-16-16 | PASS |
| D1-D3 | фиксаторы 16/20/25 различимы; 16=16 | PASS |
| E1-E3 | CV ≠ CVL ≠ C; CV=CV | PASS |
| F1-F4 | **гарды**: DN-поведение сохранено (=, ≠, пропуск, нейтраль) | PASS |
| F5 | те же DN+сечение, отличается только № ГОСТ → `+1` (ГОСТ ≠ размер) | PASS |
| F6 | спека «Дн57», счёт голый «57» → `0` (не штрафуем, тот же Ø) | PASS |
| F7 | совпадение сечения важнее опущенного префикса «Дн» → `+1` | PASS |
| G1-G4 | `extractMarkingFeatures`: config/cross/dn/без-ГОСТ-размеров | PASS |

## Гейт «Готово» (моя часть)
- ✅ Новые red-тесты зелёные (25/25).
- ✅ Существующий тест-сьют матчера зелёный: `npm run test:spec-pdf` = **7/7** (регресс 0). Иного юнит-сьюта матчера в репо нет.
- ✅ `npm run build` (backend, tsc) — чисто (exit 0).

## ВАЖНО — чего НЕ заявляю
НЕ утверждаю, что фаза закрыла #475/#476/#478 и т.п. Критерий успеха — синтетические тесты. Привязка к реальным замечаниям (вкл. гипотезу владельца «#475 = мис-распознавание ВХОДНОЙ спеки, строки съехали») — отдельная сверка с исходными спеками, ПОЗЖЕ.

## Что должен проверить ОРКЕСТРАТОР на гейте после мерджа
1. **Прод-регресс-тривайр (обязателен):** подтверждённые матчи проектов **6 и 11 не упали**. Это вне моего скоупа (прод/БД не трогаю).
2. Перепрогнать `npm run test:matcher` + `npm run test:spec-pdf` + `npm run build`.
3. **Зоны риска для прод-регресса (на что смотреть прицельно):**
   - **`sizes` (голые целые) — наибольший риск ложных `-1`.** Срабатывает только когда у обеих сторон есть голые целые И множества НЕ пересекаются. № ГОСТ/ТУ вырезаны; но напр. длины `L=3000` vs `L=6000` → `-1` (обычно корректно). Если упадут подтверждённые матчи — первый подозреваемый здесь.
   - **`extractDnValue` теперь DB-free** (был через `normalizeForMatching`). Для цифр DN поведение эквивалентно в реалистичных случаях (сверена обратная совместимость по сепараторам и ГОСТ-скобкам). Одно намеренное смягчение: пропуск DN больше НЕ штрафует, если счёт упоминает то же число голым размером (было `-1`, стало `0`).
   - **Больше элементов получают DN** (кириллица/x-суффикс) → меняется группировка дублей в `routes/matching.ts:124` (ключ `…|DN<n>`). Это улучшение, но проверить детектор дублей.
4. **Перф (необязательно):** `getStructuralScore` пересчитывает признаки счёта на каждую пару (spec×inv) — тот же паттерн, что был у `getDnScore`, и теперь DB-free (вероятно быстрее). При больших проектах можно мемоизировать признаки счёта вне spec-цикла.

## STOP / split-флаг
**Нет.** Уложился в ёмкость, развилок не было, миграция схемы БД (колонки `marking`/`type_size` в `invoice_items`) НЕ потребовалась — токенов из имён хватило на все пять классов признаков.

---

# ФИКС-РАУНД 1 (после 5-move гейта) — ГОТОВО

> Применён ПОВЕРХ базовой фазы на той же ветке `feat/matcher-marking-discriminator`. Red-first соблюдён. Без push/деплоя; прод/БД/проект 11 не тронуты. STOP/split НЕ потребовался. Миграция БД НЕ нужна.

## Изменённые файлы (этот раунд)
| Файл | Что |
|---|---|
| `backend/src/services/matcher.ts` | блокер (сильный/слабый признак) + габаритные пары + десятичная запятая + артикульные коды + нормализация компонент сечения |
| `scripts/test-matcher-marking-discriminator.mjs` | +16 тестов (H/I/J/K/L/M); итог 41 (было 25) |
| этот файл | итог раунда |

## Блокер — починен (главное)
`getStructuralScore` разделил признаки на **СИЛЬНЫЕ** (DN / сечение `AxB` / конфиг `NN-NN-NN` / буквенная марка) и **СЛАБЫЙ** (голые целые `sizes`).
- Конфликт по сильному → `-1` (другой товар) — сохранено.
- **Согласие по сильному → `+1`, и слабый конфликт его БОЛЬШЕ НЕ перебивает.** Это и есть фикс: `Труба Дн57х3,5 6000` vs `…3000` теперь `+1` (диаметр+сечение совпали, отличается лишь длина реза), хотя раньше был `-1`.
- Сильного нет вообще → голое число всё ещё различает: `Радиатор 500` vs `600` → `-1`.
Корень устранён: убран единый `note()`-цикл, где слабый `sizes`-конфликт делал `return -1` наравне с диаметром.

## Продуктовое правило владельца — соблюдено
- **Габариты = идентичность.** Пары `«3600 на 1200» / «3600х1200» / «3600*1200» / «3600x1200»` → одна сущность сечения `3600x1200` (упорядоченная, точная, любой разделитель). Разные габариты радиатора → `-1`; общая одна сторона (общий «1200») ложного «совпало» НЕ даёт (раньше давала: I1 на коммите возвращал `+1`).
- **Буквенные марки значимы** — `CV` ≠ `CVL` сохранено (M1).
- **Несущественное число не убивает матч** — длина реза/бухты не отменяет совпадение трубы/кабеля (H1/H2).

## Мелочи из гейта — починены
- **Десятичная запятая:** comma→dot перенесён В НАЧАЛО `normalizeEngineeringTokens` + каждое DN-правило теперь съедает дробный хвост `(?:\.\d+)?`. `Дн108,0` → DN=108 без лишнего размера `0`; `Дн57,5` vs `Дн57,8` → `+1` (номинальный DN=57, не конфликт).
- **Артикульные коды:** новый `ARTICLE_REF_RE` (keyword-gated `арт/артикул/кат/каталог/код` + digit-anchored) вырезает каталожные номера до извлечения размеров. `арт. 123` vs `арт. 456` → `0` (нейтрально), а не ложный `-1`. Обычные слова (`кодовый`, `каток`, `стандарт`) не трогаются — проверено пробами.
- **Доп. закалка (моё):** компоненты сечения нормализуются численно (`Number`) — `108,0х4,0` и `108х4` оба дают `108x4` (нет форка по хвосту `.0`), а реальная дробь `3.5` сохраняется. Закрывает латентный ложный конфликт, найденный adversarial-пробой.

## Red-first — соблюдён
СНАЧАЛА добавлены 16 тестов, прогон на коммите 2676b43: **10 падали** (H1/H2 блокер, I1-I4 пары, K2/K4 десятичные, L1/L2 артикул), **6 проходили-гарды** (J1, K1, K3, M1). После фикса — все зелёные. K5/K6 (форк `.0`) добавлены под закалку сечения и подтверждены пробой как red-before.

| Группа | Проверяет | До | После |
|---|---|---|---|
| H1/H2 | сильное согласие не убито голым числом (рез/бухта) | -1 | **1** |
| I1-I4 | пара = сечение; общая сторона ≠ ложное совпало | 1/0/0/null | **-1/-1/-1/"3600x1200"** |
| J1 | голое число решает при отсутствии сильного (радиатор) | -1 | -1 (гард) |
| K1-K6 | десятичная запятая, номинальный DN, форк `.0` | смешано | **all green** |
| L1/L2 | артикул не размер | -1/[123] | **0/[]** |
| M1 | марка не ослаблена (CV≠CVL) | -1 | -1 (гард) |

## Гейт «Готово» (моя часть)
- ✅ Новые red-тесты зелёные; весь сьют **41/41** (`npm run test:matcher`).
- ✅ Существующий сьют матчера зелёный: `npm run test:spec-pdf` = **7/7** (юнит-регресс 0).
- ✅ `npm run build` (backend, tsc) — чисто (exit 0).

## ВАЖНО — чего НЕ заявляю
НЕ утверждаю закрытие конкретных замечаний (#475/#476/#478 и пр.). Критерий — синтетические тесты. Гипотеза владельца «#475 = мис-распознавание ВХОДНОЙ спеки» в силе; привязка к реальным ID — отдельная сверка с исходными спеками, ПОЗЖЕ.

## Что проверить ОРКЕСТРАТОРУ (зоны риска прод-регресса)
1. **Прод-регресс-тривайр (обязателен):** подтверждённые матчи проектов 6 и 11 не упали. Вне моего скоупа.
2. Перепрогон `npm run test:matcher` (41) + `npm run test:spec-pdf` (7) + `npm run build`.
3. **Изменён ОБЩИЙ путь `normalizeEngineeringTokens`** (comma→dot в начало + DN-хвост `(?:\.\d+)?`). Влияет на `normalizeForMatching` (правила/счета/похожесть). Для нормальных строк эквивалентно (проверено); чистый эффект — у `Дн57,5`/`Дн108,0` исчезает «фантомный» токен дроби. Смотреть, не просели ли rule-матчи на DN-с-дробью.
4. **Шире `CROSS_RE` (2-й компонент 1→4 цифры).** Больше пар распознаётся как сечение (листы `1250x2500`, радиаторы). Возможен переход нейтраль(0)→конфликт(-1) у товаров, различающихся полным габаритом — обычно корректно, но проверить детектор дублей и подтверждённые габаритные матчи.
5. **Разделитель «на»** включён ТОЛЬКО в структурном пути. Известная узкая грань: `«500 на 10 секций»` склеится в сечение `500x10` (высота×секции). Редкая формулировка, высоты по-прежнему различаются; задокументировано как приемлемый риск.
6. **Перф:** `getStructuralScore` по-прежнему считает признаки счёта на каждую пару (как раньше). При больших проектах — мемоизация вне spec-цикла (необязательно).

## STOP / split-флаг (раунд 1)
**Нет.** Уложился в ёмкость; правки только в `matcher.ts` + тест-файл; миграция БД не понадобилась.
