# Стартовый промпт — Оркестратор: гейт+деплой парсера и каталога (2026-06-07)

> Скопируй в новый чат, открытый в `C:\Users\home\vscode101\budget-automation`. Ты — **оркестратор** (агент «Арта»): код сам НЕ пишешь, ведёшь процесс, фазы — воркер-чаты по бриф-файлам, гейтишь как diff/факт. Контекст держи чистым (детали — воркерам).

## Прочитай ПЕРВЫМ (память — там вся живая нить сессии)
- `project_masterplan_decisions_2026-06-05.md` — **главный журнал** всех решений/находок (читать целиком).
- `reflection_2026-06-06_verity_endtoend_sokoliy` (AI-матчинг 89.3% работает, узкое место НЕ чтение), `reflection_2026-06-07_canonical_kb_phase1` (каталог 0→42.9% durable), `reflection_2026-06-07_parser_ai_hierarchy` (дети 76→87%), `reflection_2026-06-06_spec_position_probe` (у Сокольих номеров НЕТ by design; Ласточка — номера есть).
- Правила: `feedback_no_corrupt_through`, `feedback_evidence_before_claims`, `feedback_no_action_without_confirmation`, `feedback_arta_autonomy_and_roles`, `feedback_shared_worktree_branch_hazard`.
- Мастер-план: `docs/plans/active/master_plan_phased_2026-06-05.md`.

## Точка git
- `main = 3829e4b` (задеплоено: matcher marking-discriminator). 
- **Две ветки НЕ запушены:**
  - `feat/parser-ai-hierarchy` (`66baca0`) — ИИ-якорь иерархии родитель/ребёнок в парсере спеки + блок-гейт качества + Gemini-фолбэк. **5-move = FIX (2 блокера).**
  - `feat/canonical-kb` (`59daecb`) — канонический каталог-паспортов фаза 1. Гейт честности PASS (42.9% durable). **5-move ещё НЕ прогнан.**
- ⚠️ Общий worktree, параллельные воркеры меняли ветку — ВСЕГДА `git rev-parse --abbrev-ref HEAD` перед коммитом/переключением (см. `feedback_shared_worktree_branch_hazard`).

## Следующие шаги (по порядку)
1. **Парсер — фикс 2 блокеров.** Бриф готов: `worker_brief_parser_ai_hierarchy.md`, раздел **«ФИКС-РАУНД 1»**. Владелец запускает в воркер-чате: «выполни раздел ФИКС-РАУНД 1 в docs/plans/active/worker_brief_parser_ai_hierarchy.md». Фиксы: (M2-F1) bulk-эндпоинт `specifications.ts:316` тоже применяет hardBlock; (M2-F2) `gigachatSpecFromPdf.ts:723` НЕ кеширует low-quality hardBlock-результат (иначе «422 навсегда»).
2. **После фикса — ПОЛНЫЙ повторный 5-move** на новом коммите (владелец явно просил весь цикл, не таргетный; правило re-gate-after-hardening). Скилл `pre-deploy-check`.
3. **5-move на `feat/canonical-kb`** (59daecb) — ещё не гнали. Безопаснее (matcher не тронут, таблицы пустые), но гейт обязателен.
4. **Решение деплоя (владельцу):** парсер и каталог — по очереди (два чистых «до/после») или вместе. Каталог = «спящий актив» нулевого риска (прод не меняется до бутстрапа); парсер меняет UX (новый блок-422). Плюс порог блок-гейта: старт 50% vs агрессивно 30% (реальные файлы проходят оба).

## Что уже доказано (не переоткрывать — это факты сессии)
- **Матчинг работает** ~89% через рабочий Gemini/OpenRouter (НЕ флаковый GigaChat — тот только парсер). Голый durable-матчинг = 0%.
- **Каталог (А)** даёт **42.9% durable БЕЗ ИИ** на тех же парах (0%→43%) — компаундящий актив, стратегический пивот доказан.
- **Парсер (B' через ИИ)** — привязка детей 76→87% (изолировано), сквозной 12→66%. Гипотеза владельца (ИИ размечает родитель/ребёнок по смыслу) подтверждена.
- **Чтение в основном решено** (pdfplumber+fitz, кириллица ок, ИТЕСА-фикс общий). Единственная была дыра — парент-чайлд линковка (её и чинит парсер-ветка).
- **Знание глобально, переживает удаление проекта**, но это «фото строк» без обобщения (→ каталог А это и лечит).

## Ограничения (во всех фазах)
- confirmed 268/150 (пр.6/11) уронить нельзя; регресс-тривайр = свежий per-project счёт подтверждённых (НЕ старый «333/268/44»).
- Проект 11 НЕ перезаливать. Ничего в прод без «ок» владельца. `npm run build` + 5-move PASS перед push. Коммиты — ASCII, только именованные файлы.
- CARRY (занесены, не блокеры): Gemini/spec rate-limit; рост таблиц знаний → matcher грузит все (перф) + нет чистки; цифровой parent_name_hint; section-substring 0.9; новые шаблоны → ложный bareOrphan.

## Параллельно висит (депри­оритизировано владельцем)
14 неразобранных операторских замечаний (A1 уже классифицировал по корню; владелец: оператор реакции по ним не ждёт, доверие вернёт работающая система).

## Первый ход нового чата
Подтверди готовность, дождись от владельца «готово (фикс)» по парсеру → полный 5-move → доклад с вердиктом → решение деплоя. Роль — оркестратор, фокус сверху.
