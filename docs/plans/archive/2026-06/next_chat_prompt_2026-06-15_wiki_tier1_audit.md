# Промт следующего чата: Tier-1 порядок в «вики» (2026-06-15)

> Тип: **гигиена документации** (НЕ код, НЕ прод-логика). Правило «один план — один чат».
> Источник задачи: `docs/plans/archive/2026-06/cleanup_brief_plans_wiki_2026-06-15.md` (раздел «Tier-1 порядок в вики»).
> Этот файл сам уезжает в `archive/2026-06/` после выполнения.

## 0. Что прочитать ДО любого действия

- `MEMORY.md` (★-блок «Старт сессии»), особенно:
  - `feedback_verify_resources_and_prior_art_before_acting.md`
  - `feedback_reality_check_after_every_proposal.md`
  - `feedback_no_action_without_confirmation.md`
- Действующий стратегический план: `docs/plans/active/PLAN_track_A_B_metric_gated_2026-06-11.md`
- Этот промт целиком.

## 1. Цель и scope

Привести **верхний слой документации** в соответствие с фактическим кодом и единой точкой входа. Конкретно:

### 1.1. Создать `architecture/INDEX.md`
Связывает 9 разделов архитектуры в один навигационный лист + единый «start here»:
- `01-обзор/` → `02-компоненты/` → `03-данные/` → `04-потоки/` → `05-интеграции/` → `06-стек/` → `07-безопасность/` → `08-развертывание/` → `09-решения/`
- В шапке INDEX: маршрут чтения для нового человека — `README.md` → `architecture/INDEX.md` → `docs/plans/active/PLAN_track_A_B_metric_gated_2026-06-11.md` → `.business/INDEX.md` → `research/` (если есть).
- На каждый из 9 каталогов — одна строка: что внутри, главный файл (если есть).

### 1.2. Освежить устаревшие факты в `README.md`

Сейчас README описывает стек **середины мая** и врёт по двум пунктам. Проверить и поправить:

| Что | Сейчас (врёт) | Должно быть (факт) | Источник правды |
|-----|---------------|--------------------|-----------------|
| Парсер PDF | `pdf-parse` | **pdfplumber-first** (с 2026-05-14), `pdf-parse` остался как fallback (если остался) | `backend/src/gigachatSpecFromPdf.ts` (или его аналог); `docs/plans/archive/2026-05/` step1 handoff |
| Матчинг | `string-similarity (Dice coefficient)` | **+ Gemini-tier** (фаза 8.1, 2026-05-10) поверх Dice + правила | matcher (искать `gemini` в `backend/src/`); архив `worker_brief_lastochka_*`, `IMPLEMENTATION_LOG.md` фаза 8.1 |

**КРИТИЧНО:** не писать «pdfplumber заменил pdf-parse», если pdf-parse всё ещё в коде. **Сверять с кодом**, не с памятью.

### 1.3. Освежить `architecture/01-обзор/` и `architecture/06-стек/`
Добавить **pdfplumber как слой 0** в схему/стек. Не переписывать всё — только врезать недостающее.

## 2. Что СВЕРЯТЬ с кодом (паранойя)

Перед каждой правкой README/architecture:
1. `grep -r "pdfplumber" backend/src/` — где реально вызывается?
2. `grep -r "pdf-parse" backend/src/` — остался ли в проде?
3. `grep -rE "gemini|openrouter" backend/src/` — где Gemini-tier?
4. Если код противоречит брифу — **верь коду, обнови документ под код**, а не наоборот.

## 3. Что НЕ трогать

- **Датированные хендоффы и ретроспективы** в `docs/plans/references/` и `retrospectives/` — это история, не текущее состояние.
- **Прод-параметры** (IP `5.42.103.63`, порт, путь к БД) — по брифу совпадают ✓; не «улучшать».
- **Архив планов** `docs/plans/archive/` — там 80 файлов в `2026-06/` после чистки 2026-06-15, не трогать.
- **Стратегический план и материалы текущей фазы** в `docs/plans/active/` — не трогать.

## 4. Гейт перед коммитом

1. **Doc-only** — никаких изменений в `backend/src/`, `frontend/src/`, схемах, тестах.
2. **Не запускать** `pre-deploy-check` (это не прод-логика и не data-deploy).
3. **Не запускать** `npm run build` (правила «build before push» — для кодовых изменений, тут только Markdown).
4. **`git add` именами файлов**, не `-A`/`.`.
5. **ASCII commit message** (PS 5.1 ломается на кириллице; даже из Bash на Windows надёжнее ASCII).
6. **Push** в текущую ветку (`fix/gigachat-model-tier` или своя — уточнить у владельца до push, если ветка иная).

## 5. Definition of Done

- `architecture/INDEX.md` создан, ссылается на все 9 разделов + единый «start here».
- `README.md` обновлён: парсер PDF и матчинг описаны **в соответствии с реальным кодом** (не с памятью).
- `architecture/01-обзор/` и `architecture/06-стек/` врезаны pdfplumber-слой (если он есть в коде).
- `IMPLEMENTATION_LOG.md` дополнен записью `## 2026-06-XX — Tier-1 wiki audit` (формат как у предыдущих записей).
- Этот файл (`next_chat_prompt_2026-06-15_wiki_tier1_audit.md`) перенесён в `docs/plans/archive/2026-06/` (как и сам исходный cleanup_brief).
- Коммит создан и запушен.
- `plans:check:strict` проходит (уже зелёный после чистки 2026-06-15; не должен покраснеть из-за +1 архивного файла).

## 6. Память — НЕ трогать

Память актуальна. Указатели в `MEMORY.md` валидны. **Не плодить новые мемо** про wiki — этого не требуется.

## 7. Что НЕ делать в этом чате

- Не возвращаться к чистке `active/` — она сделана 2026-06-15, коммит `225a660`, бриф в архиве.
- Не предлагать «давайте заодно отрефакторим architecture» — расползание = смерть плана.
- Не дописывать новые блоки в README — только правка двух устаревших строк.
- Не создавать новые планы — этот промт = план данного чата.
