# Воркер-бриф — BUILD: обобщённый парсер «чистого представления» (вычитание вариант-маркеров)

**Тип:** ПРОДКОД (spec-парсер). **Worktree:** `C:\Users\home\vscode101\budget-automation-spec-repr` (ветка `feat/spec-clean-repr`, базис `origin/main`=0c41f06). **НЕ деплой** — собираешь+меришь на ветке; деплой = отдельный гейт оркестратора + «ок» владельца.
**Оркестратор:** Арта (гейтит diff/факт, ведёт память). **Ты:** build-воркер. Пишешь `*_result.md`, НЕ память.

## 0. Контекст: что УЖЕ доказано (не перепроверять — это вход)
Замер-гейт `worker_brief_lastochka_clean_repr_generic_gate_result.md` доказал: ОБОБЩЁННОЕ вычитание вариант-маркеров из ключа матча (G1, БЕЗ литерал-anchor, БЕЗ класс-гейта) даёт на пр.11 durable **2.7%→53.3%** / AI-ON **26.7%→66%** (~101%/98% от anchored), **0 регрессий**, multi-project не-регрессия чиста. **Матчинговый выигрыш доказан — твоя задача НЕ передоказать его, а ПРОИЗВЕСТИ это представление в ПАРСЕРЕ.**
- Доказанный лексикон G1 (СТАРТОВЫЙ, productionize): `C:\Users\home\vscode101\ckb2\scripts\verity-lastochka-clean-generic.mjs` строки 121–149 (`G1_PATTERNS` + `reconstructG1`). Прочитай как референс.

## 1. Цель
1. **Forward (главное):** spec-парсер при разборе позиции кладёт в `full_name` ЧИСТЫЙ продукт, а снятые вариант-маркеры (исполнение / подключение / `dп=N мм` / `Q=…Вт` / этаж / хвостовой `N Вт`) → в `characteristics`. Так НОВЫЕ загрузки получают чистый ключ матча. **matcher.ts НЕ трогать** (он уже `max(sim(name),sim(full_name))` + тир characteristics).
2. **Existing пр.11 (CASCADE-safe re-split):** для УЖЕ загруженных позиций — применить то же вычитание к сохранённому `full_name`, маркеры → `characteristics`, обновить НА МЕСТЕ. **БЕЗ re-upload** (re-upload сотрёт 86 ручных связок / 150 confirmed через CASCADE). Это ОТДЕЛЬНАЯ операция от forward-фикса (урок VAT-фикса: forward-protection и data-repair — разные операции, repair — с оператором). Спроектируй + замерь офлайн (replay на temp-копии БД), НЕ применяй к проду.

## 2. Реализация
- **Чистый модуль-вычитатель** (pure, тестируемый): `extractVariantMarkers(name) -> { cleanName, markers }`. Лексикон = АТРИБУТ-СИНТАКСИС (productize G1_PATTERNS), документирован, расширяем. **no-hardcode:** ни одного литерала товара/поставщика (нет «Радиатор/Конвектор/EVRA/Tepla»). Помести так, чтобы покрыть ВСЕ пути парсинга спеки (Gemini И GigaChat И bulk-upload) — найди единый chokepoint, где собираются `full_name`/`characteristics`/пишутся `specification_items`. Сначала ЛОКАЛИЗУЙ точку построчным grep, доложи её.
- **Гейты no_corrupt_through (обязательны):**
  - Если вычитание ОПУСТОШАЕТ строку (чистые маркеры без головы товара — bare-orphan) → оставить ОРИГИНАЛ, не пустую (как guard в `reconstructG1`).
  - НЕ снимать голый DN/Ду/Дн/Ø (на трубах/опорах это различитель). Паттерн `dп=N мм` требует именно `dп=` — НЕ трогает `Дн57х3,5`/`Ду15`. Проверь это тестом.
  - Качество парса гейтить по **bareOrphanFraction** (НЕ orphanFraction — см. `reflection_2026-06-07_parser_ai_hierarchy`); если доля опустошённых выше порога — не корраптить, флажок на review.
- Маркеры в `characteristics` добавлять, НЕ затирая существующие (склейка, без потери).

## 3. Замер (на РЕАЛЬНЫХ данных, не на обещаниях)
1. **Корректность трансформа:** unit-тесты на `extractVariantMarkers` (вкл. кейсы: «Левое исполнение. Боковое подключение dп=15 мм» → чистый + маркеры; «Дн57х3,5» → НЕ тронут; bare-orphan → оригинал; «РКУ этажный» → «этажный» НЕ снят, т.к. не digit-led). + интеграция на доступных spec-PDF/фикстурах: чистый `full_name`, 0 коррупции, 0 опустошённых.
2. **Офлайн-бенчмарк replay пр.11:** прогони существующий харнесс гейта (или его эквивалент) на ВЫХОДЕ твоего парсера/re-split (re-split сохранённого `full_name`) → durable@1 должен лечь около 53.3% (или объясни дельту). Это и есть реалистичный замер (EVRA-PDF пр.11 локально НЕТ — кэш-PDF = другой документ).
3. **Multi-project НЕ-регрессия + ТРИВАЙР (критично — меняешь вход для ВСЕХ):** re-parse/re-split доступных проектов → 0 коррупции; **confirmed 268 (пр.6) / 150 (пр.11) НЕ падают**; ни одного match-регресса. Доложи `bareOrphanFraction` и сколько строк тронуто на каждом домене.
4. **Лев/Прав риск:** где снятие «исполнение» из ключа может дать прод-неоднозначность лев↔прав (два зеркальных исполнения по одной цене) — задокументируй, не замалчивай.

## 4. Жёсткие ограничения
- Работай в worktree `budget-automation-spec-repr` (ветка `feat/spec-clean-repr`). Первый шаг — `npm ci` (свежий worktree без node_modules). Перед push-готовностью — **`npm run build` (НЕ только tsc) ЗЕЛЁНЫЙ**.
- **matcher.ts НЕ трогать.** **Ничего в прод** (никаких push в main, никаких прод-мутаций — пр.11 НЕ перезаливать, replay только на temp-БД).
- `no-hardcode`, `no_corrupt_through`, `evidence-before-claims`. ASCII-коммиты, ТОЛЬКО именованные файлы (без `git add -A`/`.`). Перед КАЖДЫМ коммитом `git rev-parse --abbrev-ref HEAD` (== feat/spec-clean-repr). **Коммить инкрементально** (сессия может свернуться — прогресс должен жить на ветке).
- Эскалируй модель/глубину при нужде (продкод на входе всех проектов — цена ошибки высокая).

## 5. Deliverable → `docs/plans/active/worker_brief_spec_clean_repr_build_result.md`
ЛЕДЖЕР: (1) какие файлы изменены + chokepoint, где вставлен вычитатель (1-строчный diff-обзор каждого); (2) лексикон + поведение guard'ов; (3) unit/интеграция — что зелено; (4) офлайн-бенчмарк replay пр.11 durable@1 (vs 53.3%); (5) multi-project не-регрессия + **тривайр 268/150** + bareOrphanFraction по проектам; (6) дизайн re-split existing-пр.11 + результат офлайн-replay (БЕЗ применения к проду); (7) Лев/Прав риск; (8) самооценка готовности к 5-move (что может поймать adversarial-проход).

## 6. Отчёт мне (финальное сообщение)
КОРОТКО: файлы+chokepoint; replay durable@1; тривайр 268/150 (цел/нет); multi-project регресс (есть/нет) + bareOrphanFraction; блокеры; ветка/коммит + путь к result.md. Детали — в result.md, без простыней.
