# Стартовый промпт для СЛЕДУЮЩЕГО чата (post-deploy, Блок A)

> Открой новый чат Claude Code в папке `C:\Users\home\vscode101\budget-automation`
> и вставь весь текст ниже (между линиями `---`).

---

Привет. Возвращаюсь к проекту budget-automation. Ты — ОРКЕСТРАТОР этого чата (продолжаем после деплоя парсер-фикса).

=== СНАЧАЛА ПРОЧИТАЙ (контекст) ===
1. Память: `memory/MEMORY.md` + последние рефлексии, особенно:
   - `reflection_2026-05-30_parser_vat_fix_deployed.md` — что задеплоено + уроки: **reparse рушит матчи и минует автодетект**; постоянный коэф = структурная причина; сверять с document total; верифицировать против реального target.
   - `reflection_2026-05-30_task15_dup_detector_deployed.md` — SSH/gh на прод НЕТ, верификация через HTTP API; `unconfirm` сбрасывает `is_selected`; 5-move отдельными субагентами.
2. Handoff (главный вход): `docs/plans/references/2026-05-30_handoff_parser_deployed_next_steps.md` — точка проекта, критические факты post-deploy, шаги A/B/C, приоритеты.
3. Для контекста бага (по желанию): `docs/plans/active/parser_h3_investigation_2026-05-30.md`.

=== ТОЧКА ПРОЕКТА ===
Парсер VAT-фикс H3 (#10/#12) **задеплоен** (commit `97332ab`, push в origin/main, авто-деплой GH Actions).
Фикс = **forward-protection**: новые счета со структурой «Сумма НДС»+«Сумма» парсятся верно (`amount`=итог строки, не сумма налога). Коэф искажения был 5.545 = 122/22 (НДС 22%).
Покрытие матчинга 88.3% (377/333/268/44) **НЕ затронуто** (деплой не пересчитывает старые данные).
North Star: «80% авто + ≤15 мин ревью».

=== ТВОЯ РОЛЬ: ОРКЕСТРАТОР ===
- Держишь roadmap, метрики, гейт pre-deploy-check, мерж в main.
- Code-ради-кода НЕ делаем: задача берётся только если двигает метрику или проверяет гипотезу.
- Можешь вести задачу сам по 4-фазному циклу ИЛИ раздать worker-чатам в их git worktree (изоляция).

=== СТАТУС (обновлено 2026-05-30 (3)) ===
**Блок A ЗАКРЫТ.** Деплой `97332ab` подтверждён с сервера (SSH по паролю РАБОТАЕТ). A.1 функционально PASS (копия ЛИДЕРГРУВ → `Сумма`=price×qty). **Счёт 30 принят как stale** (forward-protection уже защитил будущее; ремонт = риск 47 матчей ради косметики прошлого — не делаем).
**CARRY-1 ЗАДЕПЛОЕН** (`586f50f`): Python amount-keywords синхронизированы с TS.
**ФИЧА #1 «НДС ровно один раз» ЗАДЕПЛОЕНА** (`99ad9a1`, 2026-05-31): сверка vatness колонки с флагом `prices_include_vat` → НДС применяется ровно один раз (нет задвоения/потери). Гейт поймал блокер (мёртвый код на PDF-пути) → closed + интеграционный тест. CARRY: savedMapping/Excel не покрыты (по сигналу). Это была фича #1 из порядка 1→2→3→4 (контракт величины: цена/ед = с НДС ×1 × со скидкой если есть).

=== КЛЮЧЕВОЙ КОНТРАКТ ===
Добываем `unit_price_with_vat` = **с НДС (ровно один раз) × со скидкой, если скидка в счёте есть** (формы: A колонка / B позиция / C «−X%» в конце счёта). Идёт в матчинг и спецификацию. Порядок фич 1→2→3→4; #1 готова.

=== СТАТУС ВЕРИФИКАЦИИ (2026-05-31, всё закрыто) ===
Фичи #1 (`99ad9a1`) + #2 (`2533dfd`) ЗАДЕПЛОЕНЫ и ВЕРИФИЦИРОВАНЫ на проде: git HEAD=`2533dfd`, контейнер пересобран (CREATED свежий), health ok. Повторно верифицировать НЕ нужно.
⚠ Урок: GH Actions делает `git reset`, но НЕ всегда пересобирает образ → проверять CREATED-время контейнера, при старом — ручной `docker compose up -d --build` (это и потребовалось для #2; pip install pdfplumber ~17 мин).
Опц. быстрая санитарная проверка на старте: `cd /root/budget-automation && git rev-parse --short HEAD && docker compose ps` → ждём `2533dfd` + контейнер Up.

=== ПЕРВЫЙ ПРИОРИТЕТ: ФИЧА #3 — скидка «−X%» в конце счёта (форма C) ===
**Контракт:** цена/ед = с НДС (×1, #1 готово) × со скидкой (если есть). #2 закрыл форму A (колонка). #3 = **форма C**: скидка задана строкой «скидка −10%» / «−X% от итога» в конце счёта → итог снижается → каждая единица дешевле пропорционально.
**Сейчас:** `detectDiscount(pdfFullText)` детектит скидку (поле `discount_detected`), но применяется ТОЛЬКО вручную оператором через `POST /api/invoices/:id/apply-discount` (`invoices.ts:1754` — умножает price+amount на `(1-d/100)`, флаг `discount_applied`). Авто-применения при парсинге НЕТ.
**Задача #3 (решить дизайном СНАЧАЛА, read-only):** авто-применять детектированную «−X%» к строкам при загрузке ИЛИ оставить оператору? Если авто — где (в `processInvoiceFile` после нормализации НДС?), как не применить дважды (флаг `discount_applied`), как не сломать, если детект ошибочен. РИСК ВЫШЕ #1/#2 (меняет суммы строк) → отдельный тщательный гейт, свежий контекст.
**Где смотреть:** `detectDiscount` в `pdfParser.ts`; `apply-discount` endpoint `invoices.ts:1754`; `discount_detected`/`discount_applied` поля.

**Прочие CARRY (по сигналу, ниже #3):** Case I тест для #2 (lone «без скидки» + «со скидкой»>«Сумма»); CARRY-3 (НДС/scorer на savedMapping-поставщиках — фичи #1/#2 их не лечат, vatness=null); Excel-путь без classifyColumnVat/discount-scorer; формы B (скидка на позиции) = фича #4; баги счетов 32 (qty=кВт)/35 (qty↔swap).

=== ДАЛЬШЕ (если A закрыт или оператор недоступен) ===
- Блок B (parser hardening): 4 CARRY-TASK из гейта (Python amount-keywords +«итого/всего»; «Сумма без скидки»>«Сумма с НДС» → завышение бюджета; scorer на savedMapping-поставщиках; Excel-тесты) + 3 бага из Step 0 (счёт 32 qty=кВт; счёт 35 qty↔сумма swap; счёт 42 нетто/брутто НДС). Брать по сигналу/повторяемости.
- Блок C (roadmap по North Star): quick-tags наполнение (H5, 17%→80%); H2 transfer на новый проект; аналоги #8 (ждут quick-tags); покрытие 88.3%→95%.
Детали всех — в handoff.

=== ПРАВИЛА ПРОЕКТА (обязательные) ===
- feedback_no_action_without_confirmation — без явного «ок» ничего не делать
- feedback_explain_before_after — состояние → изменение → результат до действия
- feedback_build_before_push — `npm run build` чистый перед push
- feedback_commit_named_files — `git add` только именованные файлы
- pre-deploy-check skill (5-move, 5 отдельных субагентов) — PASS перед `git push origin main`
- feedback_no_hardcode
- 4-фазный цикл (Design→Implement→Deploy→Post-deploy), sub-agent верификация после каждой фазы
- Прод: API `http://5.42.103.63:3001`, деплой авто на push в main, SSH/gh недоступны → верификация только через HTTP API; помни про re-select после unconfirm
- ВАЖНО про skill: если активны worktree — `/pre-deploy-check` может тянуть СТАРУЮ копию; гоняй 5-move через `Agent` с явной ссылкой на `.claude/skills/pre-deploy-check/SKILL.md` (основное дерево)

Контекст пустой, действуй экономно. На ~60% флагни и предложи handoff.

Начни с: краткое подтверждение точки проекта (1-2 строки) + предложи план Блока A (как неразрушающе подтвердить деплой) и жди моего «ок».
