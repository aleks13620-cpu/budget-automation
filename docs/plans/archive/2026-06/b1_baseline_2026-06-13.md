# Поток Б1.1 — Baseline (ДО заливки 27 high-conf пар)

**Дата:** 2026-06-13
**Автор:** воркер (Step 1 брифа `worker_brief_2026-06-13_potok_b1_load_27_pairs_to_memory.md`)
**Статус:** Read-only снимок прода. **Никаких мутаций.**

---

## 1. Git

```
git rev-parse origin/main = c00c3a2966fe5ec033ad5295ebf9bf151931dbad
```

Совпадает с ожидаемой архистратором базой `c00c3a2`. Лить можно на эту базу.

## 2. Прод-метрики (Точность@1 / Доля памяти / Подтверждённые)

Метод: `GET /api/projects/<id>/matching`; learned_rule@1 = для каждой `specItem` берётся top-1 по `confidence`, считается если `matchType === 'learned_rule'`; confirmed-spec-count = число `specItem` у которых хоть одна позиция `isConfirmed === true`.

| Проект | spec total | learned_rule@1 (Память@1) | confirmed spec items |
|--------|-----------:|--------------------------:|---------------------:|
| 6 (тест для обучения) | 377 | **47** | 268 |
| 11 (Ласточка ОВ) | 328 | **117** | 150 |
| 12 (ЖК у БКК ОВ) | 518 | **1** | 0 |

Числа точно сошлись с базой архистратора (47 / 117 / 1 и тривайр 268 / 150).

## 3. Тривайр Потока А — скидка Form D, счёт 894066

Счёт 894066 = `invoice_id=66` (см. `GET /api/projects/12/invoices`). `GET /api/invoices/66` → 13 позиций.

Эталонные цены из памяти владельца:

| Позиция | Цена ожидаемая | Цена сейчас на проде | Статус |
|---------|---------------:|---------------------:|--------|
| Воздушный клапан АВК 600*400 КР14 (id 5166) | 9605 | **9605.4** | OK |
| Воздушный клапан АВК 800*500 КР14 (id 5167) | 13573 | **13572.6** | OK |
| Решётка АЛН 1000*600 (id 5169) | 7119 | **7119** | OK |

Скидка Form D (фикс 2026-06-12, коммит `c00c3a2`) жива. Тривайр Потока А цел.

## 4. Размер Памяти на проде

Эндпоинта-обзора `matching_rules total` нет (`/api/metrics/overview` отдаёт только project-readiness). SSH/прямой sqlite на прод недоступен.

**Размер Памяти: n/a** (не блокирует Б1.1; сравнение «до/после» делается через learned_rule@1 на пр.12, что и есть гейт §1 брифа).

## 5. Сводка

База к деплою:

- git: c00c3a2 LIVE.
- пр.6 = 377/47/268 OK
- пр.11 = 328/117/150 OK
- пр.12 = 518/1/0 OK (стартовый ноль Доли памяти — то место, откуда поедем)
- Скидка 894066: 9605.4 / 13572.6 / 7119 — все три цены живы.

Готово к Шагу 2 (генерация XLSX из 27 high-conf пар).
