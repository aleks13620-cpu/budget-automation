# Стартовый промпт — Ласточка: парсер спеки + открытые вопросы (СТОП 2026-06-04)

> Открой новый чат в `C:\Users\home\vscode101\budget-automation`, роль — фуллстек/оркестратор (агент «Арта»). SessionStart-хук сам покажет очередь неразобранных замечаний.
> **ПЕРЕЧИТАЙ ПЕРЕД СТАРТОМ** (память): `feedback_evidence_before_claims` (главное!), `reflection_2026-06-04_lastochka_parser_rca_and_rigor`, `reflection_2026-06-04_operator_trust_and_strategy_risk`, `reflection_2026-06-04_telegram_f3_ipv6_retry_activated`.

## Точка проекта (git — ПЕРЕПРОВЕРЬ `git fetch origin`)
- `main = 5e751ff` (= origin/main, задеплоено и верифицировано).
- Диаг-ветка `diag/lastochka-spec-llm-test` (`eeabc83`, `7ec7d37`) — read-only тест парса, **НЕ в main**.

## ✅ Сделано и задеплоено этой сессией
- **F3 Telegram-бот АКТИВИРОВАН** (= задача 2 предыдущего промпта). Бот `@Artasamara_bot`, группа «Нейронка» `chat_id=-5155954687`. Верифиц. вживую: 3/3 `🆕` + `✓`. Коммиты: `02d7c0a` (enable_ipv6 в docker-compose — РФ блокирует Telegram по IPv4, контейнеру нужен IPv6), `5e751ff` (ретраи в telegramNotify — путь нестабилен ~15%). Токен владелец перевыпустил. Подробно: `reflection_2026-06-04_telegram_f3_ipv6_retry_activated`.

## ❌ НЕ сделано / открыто

### Задача 1 предыдущего промпта — F5 cron-мониторинг — **НЕ НАЧАТА**
Сессия целиком ушла в бота→IPv6-дебаг→разбор парсера. F5 (запланированный агент, дайджест неразобранных в Telegram) не тронут. Бот теперь готов — F5 имеет смысл.

### Ласточка (проект 11): парсинг/распознавание спеки — НОВЫЙ КРУПНЫЙ БЛОК (по операторскому разбору)
- **A1 — слипание позиций (КОРЕНЬ), root-caused, НЕ исправлено.** PDF-спека → `gigachatSpecFromPdf.ts:linkPdfParentChildren` (180-280); родитель якорится ТОЛЬКО по `position_number` (:189), а в этом PDF все 328 = null → строки-наименования удаляются как «продолжения» (:222-235), атрибутные («Левое/Правое исполнение… dп=15», unit=шт) → сироты (:273). На проде: **73 сироты** (#326/#433/#468-471) + **117 склеек со сдвигом**. Доказано кодом+прод. **Есть red-фикстура** `backend/tests/fixtures/spec-pdf/01_radiator_variants_no_position` + `scripts/test-spec-parent-child.mjs` (**PRB-008** — баг был известен). Глубже: `scripts/extract_pdf_table.py:infer_mapping_from_data` не детектит колонки.
- **A2 — количество** (297/328 NULL; выжившие 31 — мусор «Q=Вт» из чужой колонки) — следствие A1 + маппинга, не отдельный баг.
- **Направление фикса — C+E (доказано кодом):** C = доверять структуре LLM (не перемалывать её хрупким `linkPdfParentChildren` — `mapPdfItemsToRows` :333); E = сделать `evaluateSpecPdfParseQuality` **блокирующим** (сейчас advisory; `specifications.ts:106` вставляет мусор всё равно → нарушение `feedback_no_corrupt_through`). A (патчить паттерны) — **невозможно** (сигнала границ нет в данных). B (bbox)/D (шаблоны) — дороже/не масштабируются. **Владелец направление НЕ финализировал.**
- **C-тест НЕ завершён.** GigaChat-2 (Lite) на конвекторной под-спеке (270КБ) дал 7 поз. чистой структуры (7/7 номер+кол-во, 0 сирот), pdfplumber — 0. НО это **не** полная 328-спека → **полнота C НЕ доказана**.
- **НЕТ критерия качества парса** (владелец справедливо указал) — надо задать ЭТАЛОН Ласточкиной спеки (истинное число позиций + 5-10 контрольных с кол-вом) и мерить полнота/чистота/поля. До этого цифры = шум.

### Прод-конфиг (config-only фиксы, НЕ код)
- **GigaChat `GIGACHAT_MODELS_FILES` = Max-first** → fallback жжёт 4× `402` (Max/Pro **нет** на Lite-пакете; баланс цел — 19.9М), путь с одной моделью падает. **Фикс: `GigaChat-2` первым в серверном `.env` → `docker compose up -d`.** Предложено, владелец склонялся «да», **НЕ применено**.
- **SEC-1** (прод `/api` открыт без токена) — по-прежнему отложено.

### Незакрытые мелкие/средние (из операторского разбора)
- **D1:** Telegram-уведомление не показывает № позиции (`telegramNotify.ts` шлёт `поз.№` только если есть specItemId+position_number — проверить, передаёт ли тег-эндпоинт spec_item_id).
- **E1:** строка quick-tags («цена не та»/«не та маркировка») не отображалась оператору — баг видимости (frontend).
- **E2:** нет «выбрать/подтвердить все» позиции разом (bulk).
- **B1:** матчер путает CV/CVL («не та маркировка») — #472-478.
- **14 неразобранных замечаний** в очереди (Ласточка #319/#325/#326/#433/#472-478 + тест #182/#314/#315) — НЕ разобраны.

## Стратегические открытые вопросы (обсуждали, НЕ решены)
- **ГЛАВНЫЙ РИСК = недоверие оператора** (человеческий), риск провала ненулевой (`reflection_2026-06-04_operator_trust_and_strategy_risk`). Оператор: «слишком много разных поставщиков/счетов → стабильности не будет».
- **Разворот:** стабильный актив — НЕ per-supplier парсер, а **canonical KB + правила + структурные каталоги поставщиков + семантич./атрибутный матчинг** (ядро = матч разных номинавансов: вход ≠ поставщик).
- **«Малые победы»** отсутствуют — нужны (начать с простейшего поставщика → видимый результат; авто-подтверждать high-confidence; показывать «машина сама X% + тренд», дашборд F6 повернуть в «работа окупается»).
- Ветка **supplier-API/прайс-листы** — обсуждается с заказчиком (чистый структурный поставщик-сайд).
- **Email-ингест счетов** заложен, но без качественного парсинга/матчинга «не взлетит».

## Правила проекта (СОБЛЮДАТЬ)
- `npm run build` (backend+frontend) перед push; commit только именованных файлов; сообщения **ASCII без вложенных `"`**; `pre-deploy 5-move PASS` перед `git push origin main`; forward-only; автономия Арты (safe+gate-PASS+без-развилки→сам; развилка→спроси; рискованное→никогда).
- **`feedback_evidence_before_claims`** — никаких выводов без доказательной базы; гипотезу маркировать+верифицировать; метрики без эталона = шум. (Владелец продавил это жёстко — соблюдать строго.)
- **`feedback_no_corrupt_through`** — битое не должно течь в матчер/обучение.
- **Проект 11 (Ласточка) НЕ перезаливать** — сотрёт 86 ручных связок оператора (caCADE-delete `matched_items`). Фикс парсера = forward-only, проверять на ОТДЕЛЬНОМ тест-проекте/read-only.
- Ничего в прод/данные без «ок» владельца.

## Рекомендованный следующий шаг (на подтверждение владельца)
1. **config:** `.env` GigaChat-2 первым + `up -d` (оживить прод-fallback).
2. **эталон:** задать истинный состав Ласточкиной спеки (нет критерия — нет выводов).
3. **read-only:** прогнать ПОЛНЫЙ файл Ласточки через GigaChat-2 (диаг v2, без re-upload/кода) и померить против эталона → факт по C.
4. По результату — решить направление (C+E vs альтернативы); параллельно трек «малых побед».
5. **Не забыть F5** (исходная задача 1) — не начата.

## Артефакты
- Диаг: `scripts/diag_spec_pdf_llm_v2.mjs` (per-model + поиск PDF по содержимому), на ветке `diag/lastochka-spec-llm-test`. Запуск (read-only, без записи в БД): `git show origin/diag/lastochka-spec-llm-test:scripts/diag_spec_pdf_llm_v2.mjs > /tmp/d.mjs` → `docker compose cp /tmp/d.mjs app:/tmp/d.mjs` → `docker compose exec -T app node /tmp/d.mjs`.
- Прод-доступ: SSH `root@5.42.103.63`, проект `/root/budget-automation`; `.env` рядом с docker-compose.yml (token'ы Telegram/GigaChat там, `git reset --hard` его не трогает). SSH-сессия рвётся (idle) — команды иногда исполняются локально, проверять приглашение `root@...` vs `PS C:\`.
