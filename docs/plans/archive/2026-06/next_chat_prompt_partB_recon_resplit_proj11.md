# Старт для нового чата — РАЗВЕДКА+ДИЗАЙН: безопасная активация чистки на существующей пр.11 (Part B · шаг 1)

> Открой новый чат Claude Code в `C:\Users\home\vscode101\budget-automation`. Первым сообщением:
> **`выполни docs/plans/active/next_chat_prompt_partB_recon_resplit_proj11.md`**
> Это **READ-ONLY + OFFLINE** разведка и дизайн. **Никаких мутаций прода.** На выходе — GO/NO-GO + точный рецепт для гейтуемого исполнения.

## Роль
Точечный recon/design-воркер. НЕ оркестратор, НЕ исполнение. Единственная задача: определить и **доказать числами** безопасный путь, которым уже загруженные спеки пр.11 получат чистый ключ матча, **сохранив связки**. Пишешь `result.md` + сводку владельцу. Прод не трогаешь.

## КАРТИНА ДО (факт)
- Форвард-фикс «чистого представления» задеплоен (`origin/main=87dd821`): **новые** загрузки чистятся, **существующие** спеки пр.11 — нет.
- Пр.11: **150 подтверждённых пар + 86 ручных связок оператора**. Их потеря недопустима (CASCADE при re-upload стирает связки — главный риск).
- Живые радиаторные жалобы (#433/#326 и др.) держатся: ключ матча у старых строк всё ещё с шумом.
- Из гейта известно: `/reparse` (`backend/src/routes/specifications.ts:478-494`) читает `raw_data` → `parseFromRawData` → chokepoint, **минуя PDF-кэш**. НО пр.11 — PDF-источник; есть ли у него пригодный `raw_data` — НЕ подтверждено.

## КАРТИНА ПОСЛЕ (твой артефакт — НЕ прод-изменение)
Доказанный, точный, **CASCADE-safe рецепт исполнения** повторной чистки пр.11:
- какой путь активации (`/reparse` по raw_data ИЛИ in-place UPDATE) — с код-доказательством, что он бьёт в chokepoint и НЕ трогает FK-связки;
- нужен ли F3 (бамп версии PDF-парсера) для этого пути;
- offline-симуляция на СНИМКЕ прод-данных пр.11: связки целы, матчинг растёт;
- чёткий **GO/NO-GO** + точные команды/эндпоинты + план отката для гейтуемого исполнения (шаг 2 — запустит оркестратор+владелец).

## МЕТРИКА (обеспечить и доказать числами — без этого работа не принята)
1. **Путь активации определён** — код-цитаты (file:line): где пр.11 `raw_data`; проходит ли `/reparse` через `applyVariantMarkersToItems`; что с FK при `/reparse`.
2. **Сохранность связок (offline, снимок temp-БД):** после симуляции — **150 подтверждённых + 86 ручных = 0 потерь, 0 осиротевших**.
3. **Прирост (offline):** durable@1 пр.11 на очищенных ключах → к **~53%** (как в `replay-spec-clean-repr.mjs`), **0 регрессий рангов**.
4. **F3-вердикт:** нужен ли бамп версии для выбранного пути — да/нет + обоснование.

## ЗАДАЧИ (точечно)
1. Установить, как пр.11 загружен и есть ли `raw_data`/`parseFromRawData`-путь (read-only снимок БД, `specifications.ts`).
2. Трассировать `/reparse`: сохраняет ли FK (`matched_items`, `parent_item_id`), бьёт ли в chokepoint, минует ли кэш — код-доказательством.
3. Offline-симуляция на снимке прод-пр.11 (temp-БД, как существующие харнессы): применить активацию, замерить п.2-3 метрики.
4. Если `/reparse` непригоден (нет raw_data) — спроектировать in-place UPDATE (опора: demo `budget-automation-spec-repr/scripts/resplit-existing-proj11.mjs`) + что нужно (F3?).
5. Дать точный гейтуемый рецепт исполнения + риски + откат.

## ЖЁСТКО
- **Прод НЕ мутировать** (только read-only GET + offline temp-БД). **Пр.11 НЕ перезаливать.**
- `evidence-before-claims` (каждое число — из прогона/снимка), `no-corrupt-through`.
- **НЕ деплой, НЕ исполнение re-split.** Только доказанный рецепт.
- Опора: `reflection_2026-06-10_predeploy_gate_spec_clean_repr` (факт: /reparse минует кэш), `reflection_2026-06-08_clean_repr_generic_gate`; харнессы в `C:\Users\home\vscode101\budget-automation-spec-repr\scripts` (`resplit-existing-proj11.mjs`, `replay-spec-clean-repr.mjs`).

## Deliverable
- `docs/plans/active/partB_recon_resplit_proj11_result.md` — ЛЕДЖЕР: путь активации (код), offline сохранность связок (числа), durable прирост (числа), F3-вердикт, **GO/NO-GO + точные команды исполнения + откат**.
- Сводка владельцу простым языком + «нести оркестратору Арте на гейт исполнения».
