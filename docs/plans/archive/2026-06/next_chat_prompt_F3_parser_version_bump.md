# Старт для нового чата — ТОЧЕЧНО: бамп версии PDF-парсера (F3 · гигиена кэша)

> Открой новый чат в `C:\Users\home\vscode101\budget-automation`. Первым сообщением:
> **`выполни docs/plans/active/next_chat_prompt_F3_parser_version_bump.md`**
> Одна хирургическая правка + проверка. На ветке, **НЕ деплой**.

## Роль
Точечный код-воркер: ОДНО изменение — поднять `SPEC_PDF_PARSER_VERSION` + доказать эффект кодом. Пишешь `result.md`. Прод не трогаешь, не деплоишь.

## КАРТИНА ДО
Семантика парсера спеки изменилась (чистка вариант-маркеров задеплоена, `87dd821`), но `SPEC_PDF_PARSER_VERSION` (`backend/src/services/gigachatSpecFromPdf.ts:~34`) всё ещё **=5**. PDF, разобранный и закэшированный ДО деплоя, при повторной загрузке вернёт **старый грязный** результат из кэша, минуя новый чистильщик.

## КАРТИНА ПОСЛЕ
`SPEC_PDF_PARSER_VERSION` = **6** → кэш-ключ меняется → любой повторный разбор PDF инвалидирует устаревший кэш и проходит через новый чистый парсер. Будущие PDF-перезагрузки получают чистый `full_name`.

## МЕТРИКА (доказать)
1. Константа поднята 5→6 (один файл, одна строка).
2. **Код-доказательство (file:line):** версия реально входит в кэш-ключ/инвалидизацию → бамп форсит re-parse. (Если окажется, что версия НЕ влияет на кэш — стоп, доложи: правка бессмысленна, нужен другой механизм.)
3. `npm run build` зелёный.
4. Ничего другого не изменилось; `matcher.ts` не тронут; forward-only (существующие данные не мутируются) → тривайр 268/150 не затронут.

## ЗАДАЧИ
1. Найти определение `SPEC_PDF_PARSER_VERSION` и ВСЕ места, где версия участвует в кэш-ключе/инвалидизации — подтвердить, что бамп реально форсит re-parse.
2. Поднять 5→6.
3. `npm run build`; доложить эффект (цитатой кода кэша) + отсутствие побочных.

## ЖЁСТКО
- Только именованный файл, ASCII-коммит, своя ветка (НЕ main). **НЕ деплой** (гейт+push — оркестратор+владелец).
- `no-hardcode`, `evidence-before-claims`.

## Deliverable
`docs/plans/active/F3_parser_version_bump_result.md` — что изменено (file:line), как версия форсит re-parse (цитата), build-статус, подтверждение «forward-only, тривайр не затронут» + 1-строчная сводка владельцу.
