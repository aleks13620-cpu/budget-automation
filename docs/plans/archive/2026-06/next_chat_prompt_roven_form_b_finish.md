# Старт нового чата — ДОВЕСТИ ДО ДЕПЛОЯ фикс парсера РОВЕН form-B (#4)

> Предыдущий чат переполнен (3b + разбор счетов + фикс + ужесточение + 2 гейта). Работа НЕ потеряна — она в ветке.
> Этот чат: прогнать пред-деплой гейт на РЕАЛЬНЫХ данных → merge → deploy → верификация. Действовать по правилам проекта, trust-but-verify каждого шага.

## ГДЕ РАБОТА (источник истины — origin, не локальный worktree)
- **Ветка `fix/roven-form-b-parser`** (нужно запушить, если ещё не — спросить пользователя). Коммиты поверх `27f1ed8`:
  - `1d93757` — Ровен form-B фикс (81 строка, net-итог, гарды групп/реквизитов, multi-page)
  - `c7e897f` — ужесточение F1/F3/F4/F5 (консервативные guard'ы для ДРУГИХ поставщиков) + тесты
  - `bc03dcc` — инструмент `scripts/compare_parser_old_vs_new.py` (пред-деплой регресс-проверка)
- **main (origin+local) = `27f1ed8`** (задеплоено: #3 detect→review + #1/#2). Фикс Ровен **НЕ в main, НЕ задеплоен**.
- Локальный worktree `.claude/worktrees/agent-a6a260a38e9a7491c` (может быть удалён) — НЕ полагаться, брать ветку из origin.

## ЧТО СДЕЛАНО (готово, провалидировано локально)
Фикс парсера для счетов «РОВЕН-Самара» (per-position скидка КОЛОНКАМИ «Цена»/«Цена со скидкой» + multi-page + групп-подзаголовки):
- Счёт 47 → **все 81 строка**, итог **net 856 579.67** (было: 54 строки, gross 1 243 453). Счёт 5244 → итог net 94 394.05 (было gross 126 221).
- Корни багов: стр.1 выбрасывалась как «реквизиты» (мега-блоб); групп-подзаголовки лезли как товары; multi-page съезд колонок (infer вместо carry); total брал первое «Итого» = до скидки.
- Ужесточение (5-move гейт нашёл риск для ДРУГИХ поставщиков): F1 групп-фильтр подтверждается member-суммой (сервис-строки «Доставка»/«Монтаж» сохраняются); F3 net-total по ВСЕМ страницам; F4 price только из «цена»-колонок; F5 carry только при структурной совместимости. **Всё консервативно — откат на старое поведение при сомнении.**
- **Контракт:** `unit_price_with_vat` = `amount/qty` (`invoices.ts:94`), НЕ колонка price → корректность держится на верных `amount`(net) + `qty`.
- Тесты ВСЕ зелёные: roven-form-b 13, group-discriminator 14, carry-compatibility 11, vat-once 31, upload 20, mapping TS 13 / py 16, build 0. 5-move гейт + точечный re-gate = **PASS, 0 FIX-багов**. No-hardcode подтверждён.
- Локальный OLD-vs-NEW (инструмент): не-Ровен данные `SAME` (Элита-3360 CHANGED только срез мусор-строк битого шрифта, Σ не изменилась). Регрессии на доступных не-Ровен нет.

## ⛔ ОТКРЫТЫЙ ГЕЙТ ПЕРЕД ДЕПЛОЕМ (главное — НЕ пропускать)
Фикс меняет ГЛОБАЛЬНЫЙ парсер → доказать на РЕАЛЬНЫХ счетах прода, что у других поставщиков не хуже (НЕ опираться на «оператор поймает» — решение по ДАННЫМ).
- Пользователь даст счета ИЛИ перезальёт на проде позже (на момент handoff — НЕ сейчас).
- Команда на сервере (`/root/budget-automation`):
  ```bash
  git fetch origin && git checkout fix/roven-form-b-parser
  python3 -X utf8 scripts/compare_parser_old_vs_new.py data/uploads --old-ref origin/main --json /root/parser_regression.json
  ```
- **Гейт:** не-Ровен счета `SAME` (или `CHANGED`, признанный пользователем улучшением) → можно мержить. Непонятный `CHANGED` у чужого поставщика → разобрать ДО merge (это и есть пойманная регрессия).

## ОСТАВШИЕСЯ ШАГИ (по правилам проекта)
1. (Если ветка не запушена) `git push origin fix/roven-form-b-parser` — НЕ main, не деплой.
2. Получить отчёт регрессии с прода (см. гейт). Чисто → дальше.
3. `pre-deploy-check` 5-move (если scope требует; ядро уже проходило) → **FF-merge `fix/roven-form-b-parser` → main** (`git add` только именованные) → `git push origin main` → деплой.
4. **Прод-верификация:** `git rev-parse HEAD`=новый SHA + **CREATED-время контейнера свежее** + health; CREATED старое → ручной `docker compose up -d --build` (память: push ≠ deployed). SSH по паролю работает у пользователя.
5. **Координация с #4-чатом** (он на паузе по парсер-коду): перезалить 47/5244 → сверка с оракулом `docs/plans/references/2026-05-31_feature4_invoice47_acceptance_oracle.csv` (47→81 строка, Σ=856 580; 5244 без регресса) + **cleanup inv 51 на проде** (forward-only: удалить+перезалить, НЕ reparse — рвёт матчи 333/268/44).
6. **PII:** реальные PDF Ровен в фикстурах (`backend/tests/fixtures/roven-form-b/`) — решение «оставить» (приватный репо, B2B-реквизиты юрлиц, не перс.данные по 152-ФЗ; нужны для оракул-теста). Возражение пользователя → gitignore до merge.

## CARRY (не блокеры, в проблем-реестр)
- Узкий F1 «not ended_cleanly→drop» (сервис-строка в середине + «Итого» на след. странице/в др. table-объекте); вложенные 3-уровневые подсекции; пустой заголовок price-колонки → мягкая деградация на gross.
- Нет читаемого не-Ровен счёта в репо (Элита — битые шрифты без ToUnicode) → кросс-поставщик валидация ТОЛЬКО через прод-инструмент. Кандидат: добавить читаемый не-Ровен счёт в фикстуры, когда появится.
- 3b (форма C, авто-применение «−X%») — ОТЛОЖЕНО (0 кейсов на проде; реальный формат скидки у Ровен — колоночный per-position, не форма C).

## ПРАВИЛА / ПАМЯТЬ
- Читать: `MEMORY.md` + `reflection_2026-05-31_roven_form_b_wip_and_handoff` + `..._orchestrator_worker_pattern` + `..._deploy_verification_gap` + `..._parser_vat_fix_deployed`.
- **Trust-but-verify КАЖДОГО агента diff'ом** — в этой сессии Агент 1 вышел за бриф (сделал F1 сверх F3/F4) и недоотчитался; поймал только diff-ревью.
- **Серийные субагенты по одному фиксу** (один файл `extract_invoice_table.py` — параллелить нельзя, конфликт). Мелкие посильные задачи, не мега-бриф (18 мин без сходимости).
- feedback_no_action_without_confirmation; feedback_commit_named_files (НЕ `-A`/`.`); feedback_build_before_push (реальный exit); pre-deploy-check перед push в main.
- #4-чат отдельный, координация через пользователя; его прод-наблюдения: `docs/plans/references/2026-05-31_feature4_form_b_PROD_parse_observations.md`.
- Прод: API `http://5.42.103.63:3001`; деплой авто на push в main, но проверять CREATED; SSH по паролю у пользователя работает.
