# Старт для нового чата — ФОРМАЛЬНЫЙ ДЕПЛОЙ-ГЕЙТ (5-move pre-deploy-check)

> Открой новый чат Claude Code в `C:\Users\home\vscode101\budget-automation` и дай первым сообщением:
> **`выполни docs/plans/active/next_chat_prompt_predeploy_gate.md`**
> Работай АВТОНОМНО. На выходе — отчёт PASS/FIX/CARRY. **Ничего в прод не пушить** — push делает оркестратор после твоего вердикта + «ок» владельца.

## Роль
Ты прогоняешь **формальный 5-move гейт** (скилл `pre-deploy-check`) на ЧИСТОМ деплой-кандидате и выдаёшь вердикт. Свежие глаза: НЕ доверяй «оно уже проверено» — активно ищи проблемы. НЕ оркестратор, НЕ деплой.

## Что ревьюишь (чистый кандидат — ровно 3 прод-файла)
- Worktree: `C:\Users\home\vscode101\budget-automation-deploy`, ветка `feat/spec-clean-repr-deploy` @ `fd752cc` (база `origin/main`=0c41f06).
- Diff: `git -C C:/Users/home/vscode101/budget-automation-deploy diff origin/main...feat/spec-clean-repr-deploy`
  → `backend/src/services/variantMarkers.ts` (новый), `excelParser.ts` (+7), `gigachatSpecFromPdf.ts` (+8). `matcher.ts` НЕ входит.

## Контекст (намерение, не вывод — проверяй сам)
Парсер спецификации теперь вычитает вариант-маркеры (исполнение/подключение/`dп=N мм`/`Q=…Вт`/`N этаж`/`N Вт`) из имени-ключа в `characteristics`, чтобы ключ матча нёс чистый продукт. Изменение **forward-only** (существующие спеки НЕ перепарсиваются деплоем). Eval-скрипты/кэши СОЗНАТЕЛЬНО исключены из кандидата (eval-обвес на main не несём).

## Как прогнать
1. Прочитай скилл `.claude/skills/pre-deploy-check/SKILL.md` и память/feedback-инварианты, на которые он ссылается (`feedback_no_hardcode`, `feedback_no_corrupt_through`, MEMORY.md инварианты: full_name/parent_item_id/confidence 0..1, тривайр confirmed 268/150).
2. Вычисли diff против `origin/main` для кандидата (команда выше) — НЕ для текущей ветки чата.
3. Прогони ВСЕ 5 ходов, **каждый — отдельным субагентом с чистым контекстом** (не сливай в один проход). Между ходами — короткое summary.
4. **Move 4 build-gate:** backend build. (Кандидат-worktree без node_modules; собери в готовом worktree с зависимостями: `npm --prefix C:/Users/home/vscode101/budget-automation-spec-repr/backend run build` — там идентичный прод-код. Если хочешь чистоты — `npm ci` в кандидате, потом build.) Зелёный обязателен; иначе вердикт = FIX.
5. **Move 5 security:** это чистая строковая трансформация без новых эндпоинтов/исходящих вызовов/SQL — но проверь сам по чек-листу скилла (ReDoS на regex-лексиконе? потеря данных? логи?).

## Особое внимание (типичные дыры именно этого diff — проверь, не поверь)
- **Полнота chokepoint:** реально ли ВСЕ пути парсинга (Gemini + GigaChat + pdfplumber-fallback + Excel upload/bulk/raw) проходят через точку вычитания? Нет ли write-пути в обход?
- **no_corrupt_through:** не опустошает ли строку (bare-orphan)? не снимает ли голый `Дн57х3,5`/`Ду15`/`Ø`? цел ли «этажный»? характеристики добавляются, не затираются?
- **Regex-лексикон:** глобальные regex со `lastIndex` (состояние между вызовами)? катастрофический бэктрекинг? кириллица+латиница смесь?
- **Лев/Прав:** снятие «исполнение» из ключа теряет различитель — где это может дать неверный матч в проде?

## Output / возврат
- Отчёт по формату SKILL.md (Move 1–5 + **Verdict: PASS | FIX | CARRY-TASKS**).
- **Финальная сводка владельцу** простым языком: вердикт, что (если) надо починить, готов ли к push.
- **Ничего не пушить.** Владелец принесёт вердикт оркестратору «Арте»; push — оркестратор + «ок» владельца.
- Если Verdict=FIX: перечисли пункты с `file:line`, НЕ чини без подтверждения владельца.
