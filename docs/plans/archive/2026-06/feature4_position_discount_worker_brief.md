# Worker-бриф — Фича #4: скидка на отдельные позиции (форма B)

> Задание для НОВОГО оркестратор-чата (запускается, пока основной чат «припаркован» на #3b — ждёт реальные form-C счета).
> ⚠ РИСК ДАННЫХ: форма B помечена в roadmap **«по сигналу, не вперёд данных»**. Урок #3: форма C дала **0 кейсов** → авто-применение отложено (#3b). **#4 МОЖЕТ быть так же → строго investigation-first.**
> Реализуй в git worktree от ТЕКУЩЕГО `main` (**27f1ed8** — #1/#2/#3 уже в main!), НЕ пушь.

## Контракт (зачем)
Ключевая величина `unit_price_with_vat` = цена/ед **с НДС (×1, #1 ✅)** × **со скидкой, если есть**. Идёт в матчинг и в итоговую смету → форма скидки должна быть учтена корректно (искажение цены единицы искажает всю смету).
Формы скидок:
- **A — скидка-колонка** — #2 ✅ deployed (де-ранк «Сумма без скидки»).
- **C — скидка «−X%» строкой в конце счёта** — #3 ✅ deployed как **detect→review** (помечает оператору; авто-применение = #3b, отложено до реальных счетов).
- **B — скидка на ОТДЕЛЬНЫЕ позиции** — **ЭТА фича #4.** Не на всю накладную (как C) и не чистой колонкой (как A), а на конкретные строки (напр.: «−15%» рядом с позицией / два прайса original+discounted в строке / часть позиций со скидкой, часть без).

## ⚠ Главный вопрос (из-за него — investigation-first)
НЕ известно: (1) есть ли форма B в реальных данных прода, (2) как она выглядит и как сейчас парсится. Roadmap прямо: «по сигналу, не вперёд данных». **Не строить обработку формы B, пока Этап 1 не покажет реальные кейсы и их вид.** Если кейсов 0 — #4, как #3b, = detect/flag или defer, НЕ авто-обработка вперёд данных.

## Текущее состояние discount-машинерии (подтвердить на Этапе 1, read-only)
- `detectDiscount(text)` `backend/src/services/pdfParser.ts:206` — ловит «скидка X%» во ВСЁМ тексте (форма C), не per-position.
- `POST /api/invoices/:id/apply-discount` `invoices.ts:1774` — ручное, множит price+amount ВСЕХ строк (whole-invoice).
- `POST /api/invoices/:id/apply-net-price-mode` `invoices.ts:1977` — множит только price, пишет `original_price` (per-line precedent — изучить, не основа ли под форму B).
- `invoice_items.original_price` — колонка существует (исходная цена до правок). Возможный носитель «цена до скидки» per-line.
- `processInvoiceFile` `invoices.ts:293` — единая точка вставки PDF+Excel (где #3 detect→review добавлен ~:669).
- `computeUnitPriceWithVat` `invoices.ts:94` — где собирается итоговая цена/ед.
- `needs_amount_review` `init.ts:35` — готовый флаг для неоднозначного на review.
- Форма B-специфичный код: **НЕИЗВЕСТЕН → Этап 1 картирует.**

## Этап 1 — Расследование + ДИЗАЙН (read-only, СТОП)
1. Как сейчас парсятся per-position скидки, если вообще? Есть ли в коде/схеме понятие построчной скидки (grep `original_price`, per-item discount, «скидк» в построчном парсинге)? Как форма B сейчас попадает (или теряется) в `price`/`amount`/`original_price`?
2. **Прод read-only (GET, API `http://5.42.103.63:3001`):** есть ли реальные счета с per-position скидкой? Как выглядят? Сколько таких? (как Этап 1 у #3 нашёл 0 form-C — определит, нужна ли обработка вообще и какой кейс реальный).
3. По результату спроектируй: если 0 кейсов → detect/flag или defer (не код вперёд данных). Если есть → как добыть верный `unit_price_with_vat` per line (скидка ровно раз, без двойного учёта), где в коде, forward-only, без хардкода; неоднозначное → `needs_amount_review`.
4. **⛔ СТОП — верни дизайн + данные оркестратору, жди «ок». НЕ кодь.**

## Этап 2 — Реализация (после «ок»)
5. Утверждённый дизайн + red→green тесты + **интеграционный тест на боевом пути `processInvoiceFile`** (юнит-зелёный ≠ wired-in). TS+Python синхронно, если затрагивает оба парсера. Excel-путь учесть.
6. Forward-only (на загрузке), НЕ reparse (рвёт матчи 333/268/44).

## Критерии приёмки
- Форма B учтена ровно один раз; нет двойного учёта; неоднозначное → review.
- Не ломает #1 (НДС ×1), #2 (форма A), #3 (форма C detect→review): тесты VAT-once **31**, upload-integration **20**, parser-mapping **13** зелёные.
- `cd backend && npm run build` чистый.

## Процесс / правила
- Worktree от ТЕКУЩЕГО `main` (`27f1ed8`). НЕ commit в main, НЕ push, НЕ reparse. Минимум файлов, перечислить.
- Оркестратор: 5-move гейт (5 субагентов, main-SKILL `.claude/skills/pre-deploy-check/SKILL.md`) → merge+push → деплой → верификация (`git rev-parse HEAD` + **CREATED-время контейнера** + health; CREATED старое → ручной `docker compose up -d --build`).
- Память (читать): `reflection_2026-05-31_feature3_detect_review_deployed` (0-кейсов→defer; detect→review как фундамент; junction-ловушка при cleanup worktree), `reflection_2026-05-31_orchestrator_worker_pattern` (unit-green≠wired-in; npm install в worktree затирает node_modules main), `reflection_2026-05-30_parser_vat_fix_deployed` (сверять с document total; reparse разрушителен; TS+Python синхронно), `reflection_2026-05-30_deploy_verification_gap` (push≠deployed).
- ⚠ **КООРДИНАЦИЯ С #3b:** #3b (авто-применение формы C) ещё НЕ сделана — припаркована в основном чате до реальных form-C счетов, живёт в ТОЙ ЖЕ discount-зоне (`processInvoiceFile`). #4 и #3b ОБА трогают discount-логику. Кто мёржит в main первым — второй ребейзит worktree от нового main. Near-term оба в investigation → конфликта нет; синхронизировать ПЕРЕД merge.
