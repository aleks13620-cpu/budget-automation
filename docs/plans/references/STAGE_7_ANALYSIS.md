# 📊 АНАЛИЗ ВЫПОЛНЕНИЯ ЭТАПА 7

## План из `docs/plans/archive/2026-04/legacy-system-implementation-plan.md` (Э7: UI подтверждения сопоставлений)

### 1️⃣ СТРАНИЦА СПИСКА ПОЗИЦИЙ (2 часа)

#### Требование: Таблица всех позиций спецификации
- **✅ РЕАЛИЗОВАНО**: MatchTable.tsx показывает все позиции
- Файл: `frontend/src/components/MatchTable.tsx`

#### Требование: Статусы (✅ подтверждено, ⚠️ требует проверки, ❌ не найдено)
- **✅ РЕАЛИЗОВАНО**: 
  - `match-row-confirmed` (зелёный фон)
  - `match-row-candidate` (жёлтый фон)
  - `match-row-unmatched` (серый фон)
- Классы в `App.css` строк 180–184

#### Требование: ФИЛЬТРЫ по статусу
- **❌ НЕ РЕАЛИЗОВАНО**
  - Нет кнопок/переключателей для фильтрации
  - MatchingView показывает все позиции без фильтрации
  - Нужно добавить: "Все", "Подтверждённые", "Требуют проверки", "Не найдены"

---

### 2️⃣ КАРТОЧКА СОПОСТАВЛЕНИЯ (4 часа)

#### Требование: Отображение позиции спецификации
- **✅ РЕАЛИЗОВАНО**: Название, раздел, характеристики видны
- Строки 119–125 в MatchTable.tsx

#### Требование: Топ-3 варианта из счетов
- **✅ РЕАЛИЗОВАНО**: 
  - matcher.ts ограничивает до 3 вариантов (строка 187)
  - MatchTable раскрывает все варианты при нажатии "▼"

#### Требование: Для каждого варианта показать:

**Требование: % совпадения (цветовая индикация)**
- **✅ РЕАЛИЗОВАНО**: 
  - confidence-badge (строки 147–149, 205–207)
  - Стили в App.css строки 191–197
  - Цветовой фон (#e0e7ff) и текст (#3730a3)

**Требование: Поставщик**
- **✅ РЕАЛИЗОВАНО**: `supplierName` показывается (строки 141, 201)

**Требование: Цена и СУММА**
- **✅ Цена**: `price` показывается (строки 144–145, 204)
- **⚠️ СУММА не показана явно**:
  - В MatchItem есть поле `amount`, но оно не выводится
  - Нужно добавить отображение суммы (amount)

#### Требование: Кнопки действий
**Кнопка "Подтвердить"**
- **✅ РЕАЛИЗОВАНО**: Кнопка "OK" (строка 151–157)
- Вызывает `PUT /api/matching/{id}/confirm`

**Кнопка "Отклонить"**
- **✅ РЕАЛИЗОВАНО**: Кнопка "X" (строка 158–164)
- Вызывает `DELETE /api/matching/{id}`

**Кнопка "Подтвердить как аналог"**
- **❌ НЕ РЕАЛИЗОВАНА**
  - План требует отдельную кнопку для "аналога"
  - Сейчас любое подтверждение создаёт rule (строки 202–213 в matching.ts)
  - Нужно уточнить: чем отличается "аналог" от обычного подтверждения?

---

### 3️⃣ СОХРАНЕНИЕ РЕШЕНИЙ (2 часа)

#### Требование: `POST /api/matches/{id}/confirm`
- **⚠️ РЕАЛИЗОВАНО с отличием**: 
  - Используется `PUT /api/matching/{id}/confirm` (вместо POST)
  - Функционально работает корректно
  - Обновляет `matched_items` и создаёт rule

#### Требование: Обновление `matched_items`
- **✅ РЕАЛИЗОВАНО** (строки 195–200 в matching.ts):
  - `is_confirmed = 1, is_selected = 1`

#### Требование: Создание правила в `matching_rules` (если аналог)
- **⚠️ ЧАСТИЧНО РЕАЛИЗОВАНО**:
  - Правило создаётся при ЛЮБОМ подтверждении (строки 202–213)
  - Нет условия "если аналог"
  - Алгоритм: берёт spec_name и invoice_name, нормализует, сохраняет как rule
  - Confidence: 0.9, times_used: 1

#### Требование: Автопереход к следующей позиции
- **❌ НЕ РЕАЛИЗОВАНО**
  - При подтверждении/отклонении позиция остаётся на месте
  - Нет механизма прокрутки к следующей незавершённой позиции
  - Нужно добавить логику "Перейти к следующей"

---

## 📋 ИТОГОВАЯ ТАБЛИЦА

| Требование | Статус | Файл | Примечание |
|-----------|--------|------|-----------|
| Таблица всех позиций | ✅ | MatchTable.tsx | Полная реализация |
| Визуальные статусы | ✅ | App.css + MatchTable.tsx | 3 состояния (confirmed/candidate/unmatched) |
| **ФИЛЬТРЫ по статусу** | ❌ | - | **НУЖНО ДОБАВИТЬ** |
| Отображение позиции | ✅ | MatchTable.tsx | название, раздел |
| Топ-3 варианта | ✅ | matcher.ts + MatchTable.tsx | раскрытие при нажатии |
| % совпадения (цвет) | ✅ | App.css + MatchTable.tsx | confidence-badge |
| Поставщик | ✅ | MatchTable.tsx | supplierName |
| Цена | ✅ | MatchTable.tsx | price |
| **Сумма (amount)** | ❌ | - | **НУЖНО ДОБАВИТЬ** |
| Кнопка "Подтвердить" | ✅ | MatchTable.tsx | OK button |
| Кнопка "Отклонить" | ✅ | MatchTable.tsx | X button |
| **"Подтвердить как аналог"** | ❌ | - | **НУЖНО УТОЧНИТЬ** |
| Сохранение решений | ✅ | matching.ts | PUT /api/matching/{id}/confirm |
| Создание rule | ✅ | matching.ts | При подтверждении |
| **Автопереход** | ❌ | - | **НУЖНО ДОБАВИТЬ** |

---

## 🚨 СПИСОК ОТСУТСТВУЮЩЕГО (5 пунктов)

1. **ФИЛЬТРЫ на странице сопоставления**
   - Кнопки: "Все", "✅ Подтверждённые", "⚠️ Требуют проверки", "❌ Не найдены"
   - Должны фильтровать rows в MatchTable

2. **Отображение суммы (amount)**
   - Поле `amount` есть в `MatchItem`, но не выводится
   - Нужно добавить колонку "Сумма" в таблицу

3. **Кнопка "Подтвердить как аналог"**
   - Требует уточнения: отличается ли от обычного подтверждения?
   - Возможно: при аналоге создаётся другой тип rule?

4. **Автопереход к следующей позиции**
   - При подтверждении/отклонении → прокрутка к следующей неподтверждённой
   - Или кнопка "Далее" для переход к следующей

5. **API endpoint для фильтрации** (опционально)
   - Можно фильтровать на фронте (js) или на бэкде
   - Сейчас GET /api/projects/{id}/matching возвращает всё

---

## 📌 КОЛ-ВО ФАЙЛОВ, НУЖДАЮЩИХСЯ ИЗМЕНЕНИЙ

- **frontend/src/pages/MatchingView.tsx** — добавить фильтры (state + UI)
- **frontend/src/components/MatchTable.tsx** — добавить amount колонку, кнопку "аналог"?
- **frontend/src/App.css** — стили для фильтров
- **backend/src/routes/matching.ts** — возможно новый endpoint для "аналога"?

---

**Дата анализа:** 07.02.2026  
**Версия:** 1.0
