# Стартовый промпт для НОВОГО чата — Фикс битых названий (битый шрифт) (оркестратор)

> Открой новый чат Claude Code в `C:\Users\home\vscode101\budget-automation`, дай мне роль
> ОРКЕСТРАТОРА и вставь текст между линиями.

---

Привет. Возвращаюсь к budget-automation. Ты — ОРКЕСТРАТОР этого чата. Делаем фикс:
**порча названий позиций при парсинге из-за битого шрифта PDF** (реальный сигнал оператора).
**СТРОГО investigation-first** — сперва точно понять масштаб и корень на реальных данных, потом
дизайн (твой чекпоинт), потом код.

=== СНАЧАЛА ПРОЧИТАЙ ===
1. Бриф (ТЗ, симптом, корень, план): `docs/plans/active/parser_broken_font_names_worker_brief.md`
2. Память: `MEMORY.md` + рефлексии:
   - `reflection_2026-06-01_roven_form_b_deployed` — последний деплой (bc03dcc); **OLD-vs-NEW дата-гейт
     на реальном корпусе** (`scripts/compare_parser_old_vs_new.py`); гейт на ХОСТЕ требует
     `pip install fitz/pdfplumber` (на прод-сервере уже поставлены); функц-проверка = re-upload боевым
     путём; дата-гейт ловит и попутные улучшения/регрессии у других поставщиков.
   - `reflection_2026-05-30_parser_vat_fix_deployed` — сверять с реальным target; reparse разрушителен
     (forward-only!); дублированную логику TS+Python синхронно.
   - `reflection_2026-05-31_orchestrator_worker_pattern` — оркестратор+субагент-в-worktree;
     **unit-green ≠ wired-in** (интеграционный тест на боевом пути); 5-move ловит то, что single-pass
     пропускает; npm install в isolation-worktree затирает node_modules main.
   - `reflection_2026-05-30_deploy_verification_gap` — **push ≠ deployed** (CREATED-время контейнера).

=== ТОЧКА ПРОЕКТА ===
Прод = `bc03dcc` (#4 Ровен задеплоен+верифицирован). Стек: Node/Express + SQLite + Docker на
**Timeweb (РФ)**. API `http://5.42.103.63:3001` — **доступен read-only прямо из среды разработки**
(`curl` работает, проверено 2026-06-01). SSH по паролю у владельца.

=== СИГНАЛ И ДИАГНОЗ (уже собрано read-only 2026-06-01) ===
Оператор «Инженер Сети Иван»: «слово радиатор по-разному пишется в распознавании счетов».
Счёт **ИТЕСА №3227**, проект 11 «Ласточка ОВ», **invoice id=59**, 186 позиций.
Факт (`GET /api/invoices/59`): «Радиатор» хранится как
`P?\xa00а0диатор / P?\xa0Kа0диатор / P?\xa0Lа0диатор` — **NBSP (`\xa0`) + посторонние символы ВНУТРИ
слова, по-разному по строкам**; артикулы (F0Z…/F0W…) — ЧИСТЫ. Признак **битого/встроенного шрифта
без ToUnicode** на кириллице (класс «Элита»).
**Корень — слепое пятно роутинга:** счёт сохранён category A, review=0, потому что ФИНАНСЫ
(qty/price/amount) извлеклись верно, а `needsGigaChat = items.length===0 || financialCoreMissing`
роутит в GigaChat при отсутствии ФИНАНСОВ — НЕ при порче ИМЁН. Мусорные имена проходят «успешно».
**Практика:** «Ласточка ОВ» (проект 11) — реальный; обучать на нём с битыми именами = засорить
матчер. Этот фикс = условие чистого обучения.

=== SCOPE / ЦИКЛ ===
**Этап 1 (read-only, СТОП на чекпоинт):**
- точные БАЙТЫ названия (сырой JSON/hex с `/api/invoices/59`) — отделить реальную порчу от мойибаки
  консоли; зафиксировать, какие символы лишние;
- PDF ИТЕСА: есть ли ToUnicode у шрифта названий? (pdffonts / fitz);
- **МАСШТАБ:** на скольких поставщиках/счетах прода встречается порча имён (read-only API);
- дизайн фикса (детектор «мусорного имени» по ПРИЗНАКУ порчи → роутить в GigaChat ИЛИ флаг
  `needs_*_review`) → вернуть оркестратору, ждать «ок». **НЕ кодить вперёд дизайна.**
**Этап 2 (после «ок»):** реализация в worktree от ТЕКУЩЕГО `main` (НЕ пушить); TS+Python синхронно
если затрагивает оба парсера; red→green + интеграционный тест на боевом пути; Excel-путь учесть.

=== ⛔ ОБЯЗАТЕЛЬНЫЙ ГЕЙТ ПЕРЕД ДЕПЛОЕМ ===
Это **ГЛОБАЛЬНОЕ изменение парсера** (имена ВСЕХ поставщиков) → как у #4, прогнать **OLD-vs-NEW
регресс на РЕАЛЬНОМ корпусе прода** ДО merge:
- на прод-сервере: `git fetch origin && git checkout <ветка>` →
  `python3 -X utf8 scripts/compare_parser_old_vs_new.py data/uploads --old-ref origin/main --json /root/parser_regression.json`
  (fitz/pdfplumber на хосте уже стоят; данные не покидают сервер).
- Не-затронутые счета — `SAME`; затронутые (битые имена) — стали ЛУЧШЕ (признано владельцем).
  Непонятный `CHANGED` у чужого поставщика → разобрать ДО merge.

=== ПРАВИЛА ===
feedback_no_action_without_confirmation; feedback_explain_before_after; feedback_build_before_push
(реальный exit); feedback_commit_named_files (НЕ `-A`/`.`; package-lock не коммитить);
**feedback_no_hardcode (детект по признаку порчи, НЕ по слову «радиатор» и НЕ по поставщику «ИТЕСА»)**;
forward-only (НЕ reparse — рвёт матчи). pre-deploy-check 5-move PASS перед `git push origin main`.
Деплой-верификация: SHA + CREATED-время контейнера + health; CREATED старое → ручной
`docker compose up -d --build`; затем функц-проверка re-upload битого счёта.
Контекст экономь; на ~60% флагни handoff.

Начни с: подтверждение точки проекта (1-2 строки) + план **Этапа 1** (read-only: точные байты +
шрифт PDF + масштаб по проду) и жди моего «ок».

---
