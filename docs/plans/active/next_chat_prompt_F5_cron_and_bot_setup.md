# Стартовый промпт — F5 (cron-мониторинг замечаний) + настройка Telegram-бота

> Открой новый чат в `C:\Users\home\vscode101\budget-automation`, роль — фуллстек/оркестратор (агент «Арта»). Создан 2026-06-03 после марафон-сессии: задеплоены дашборд метрик Фаза 3, F1, F2, F3, F6, SEC-1.
> При старте SessionStart-хук сам покажет очередь неразобранных замечаний (F2 работает).

## Точка проекта (ПЕРЕПРОВЕРЬ `git fetch origin`)
- `main` = `a2586ad` (на проде). Всё ниже задеплоено и верифицировано боевым API + bundle-hash.

## Что уже сделано (доверять)
| Фича | Коммит | Статус |
|---|---|---|
| Дашборд метрик Фаза 3 (графики) | `1416b96` | ✅ |
| F1 — FB-STATUS (статус «разобрано» сохраняется) + кнопка «Отметить разобранным» | `9d265f8` | ✅ верифиц. |
| F2 — Арта при заходе видит очередь замечаний (SessionStart-хук → `scripts/open_feedback.mjs`, локально в `.claude/settings.local.json`) | — | ✅ работает вживую |
| F3 — Telegram-уведомления о замечаниях (сразу + ack «✓ Отработано»), residency-min | `75b1802` | ✅ код на проде, **no-op до настройки бота** |
| F6 — сводка «Все проекты» + готовность во времени (`GET /api/metrics/overview`) | `007ac99` | ✅ глоб. 14.9%, проект 6 — 71.1% |
| SEC-1 — секрет-ссылка (`?k=<secret>` → Bearer; замок в `index.ts`) | `e7423a0`/`23d13b2`/`a2586ad` | ✅ код на проде, **no-op до `API_SECRET`** |

## Где состояние и правила
- **Дизайн-док:** `docs/plans/active/feedback_loop_and_finish_metric_design_2026-06-03.md` (роли, автоцикл замечаний, фазы F1–F6, решения владельца).
- **Память:** `feedback_arta_autonomy_and_roles.md` (роли оператор/владелец/**Арта**/уведомитель + правило автономии + прогресс), `reflection_2026-06-03_metrics_phase3_deployed.md`.
- **Правила:** `npm run build` обоих перед push; commit только именованных файлов (сообщения **ASCII, без вложенных `"`** — PS 5.1 расщепляет аргумент); **pre-deploy-check 5-move PASS** перед `git push origin main` (Move 5 residency — для исходящих данных); **автономия Арты** (safe-класс + гейт-PASS + без-развилки → деплой сам; развилка → спроси владельца; рискованное/corrupt-through → никогда не авто); forward-only.
- **Деплой:** GH Actions «Deploy to server» (~1 мин, обычно сам); «Plan guard» всегда красный (CI-1, игнор). Верификация — **bundle-hash отданного прода** + боевой API (loopback песочницы блокирует локальный браузер-рендер). SSH только у владельца.

## ЗАДАЧА 1 — F5: cron-мониторинг замечаний
Запланированный агент (skill `schedule` / cron-routine / `CronCreate`), который раз в N часов дёргает прод `GET /api/feedback/all?status=new` и, если есть новые неразобранные, шлёт Telegram-дайджест (канал F3) и/или открывает триаж. **Это НЕ прод-код, а планировщик.**
- Investigation-first: глянуть skill `schedule`/`CronCreate`; НЕ дублировать F2-хук (F2 = при заходе в сессию; F5 = проактивно по расписанию).
- Полезен в полную силу **после** настройки бота (есть куда слать). Если бот ещё не настроен — можно сделать F5 «тихим» (готов, активируется вместе с ботом).

## ЗАДАЧА 2 — настройка Telegram-бота (включает F3; помочь владельцу)
Активация на сервере (владелец): @BotFather → `TOKEN`; узнать `chat_id` (через `getUpdates` — помочь разобрать ответ); в серверный `.env` рядом с `docker-compose.yml`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, опц. `APP_BASE_URL=http://5.42.103.63:3001`; `docker compose up -d`. Тест: нажать тег-замечание в приложении → «🆕 Замечание …» в Telegram. (compose уже прокидывает эти env — `a2586ad`.)
- Опц. **SEC-1**: `API_SECRET=<secret>` в тот же `.env` + `docker compose up -d` + открыть `…/?k=<secret>`. После активации SEC-1 — прописать `PROD_TOKEN` для SessionStart-хука (`scripts/open_feedback.mjs`), чтобы Арта продолжила видеть очередь (иначе 401, хук фейлит мягко).

## CARRY (не блокеры)
- **C2:** `getOverview` читает всю таблицу `metric_snapshots` — SQL-кап/даунсэмпл при росте.
- **D:** «Правила» в истории дают ступеньку 0→452 (артефакт смены определения метрики, не рост) — аннотировать/бэкфилл.
- **F4 (триаж):** процедурный, в основном покрыт F2 + автономией; при желании — статус «→ задача» (PRB).
