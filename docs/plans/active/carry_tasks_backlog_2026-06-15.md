# Бэклог хвостов (carry-tasks) — приоритизированный

> Назначение: чтобы хвосты НЕ висели. Правило: каждый пункт — СВОЙ focused-чат при взятии (один план = один чат); этот файл только трекает + приоритизирует. Любой деплой = гейт 5 ходов + «ок» владельца. GigaChat — вне этого бэклога (свой промпт: `next_chat_prompt_gigachat_model_fix.md`).

## P1 — Честность метрик (главный фокус владельца) · один чат на оба (связаны)
- **В1.2 — честный замер Точности@1.** API прячет соперников у confirmed-строк → ложные 100% (тавтология). Правка: отдавать ВСЕХ кандидатов независимо от confirmed; мерить top-1 vs эталон. Деплой. → делает ключевую метрику Б1 реальной.
- **Дашборд: покрытие «подтверждено» vs «только кандидат».** Сейчас пр.13 «38%» выглядит победой при негодных кандидатах. Разделить. → честность А1 (покрытие).

## P2 — Целостность данных и CI (защита доверия) · «integrity guards» одним чатом
- **Plan guard CI падает** на каждом push — разобраться почему; failing-check маскирует реальные сбои. Дёшево.
- **guard `updateExisting`** (`matching.ts:~1887`) — не затирать `manual`/`llm_confirm` при импорте (риск порчи подтверждений, no_corrupt_through).
- **multer без limits** (`matching.ts:~14`) — лимит размера загрузки (DoS/безопасность).

## P3 — Чистки / ориентация (низкий риск)
- **пр.6 rename → «Сокольи горы ВК».** Функции rename НЕТ → мини-эндпоинт `PUT /api/projects/:id` (поле name) + деплой. Владелец просил (ориентация).
- **hardcoded path** в `export-high-conf-xlsx.ts`.
- **drift normalize / старый путь эталона** в `extract-ethalon-pairs.ts:29` (путь без `Таблицы\`).
- **мемоизация `selectableBestMatches`** (перф на масштабе — пока незаметно).

## Оппортунистично (когда придут данные)
- **parent-child — тест чистых имён** на следующем реальном счёте/спеке (читаемость для оператора; метрика Б4 «применяемость»).

## Как брать
Владелец выбирает пункт → открываем focused-чат этим пунктом (план по фазам, 5 ходов, деплой по «ок»). Приоритет по умолчанию: **P1 → P2 → P3 → оппортунистично.**

> Связано: [[feedback_one_plan_one_chat_phased]], [[project_global_metrics_dashboard]], [[feedback_no_corrupt_through]].
