# Результат: ИИ-разметка родитель/ребёнок в парсере спеки (worker_brief_parser_ai_hierarchy)

> Воркер-итог. Ветка `feat/parser-ai-hierarchy` (база = `feat/verity-harness`, но затронутые
> файлы парсера **байт-в-байт совпадают с `origin/main`** — проверено `git diff origin/main`).
> НЕ запушено, НЕ задеплоено. Оркестратор гонит 5-move pre-deploy и решает push.

## TL;DR
- Все 5 правок брифа сделаны (1–4 обязательные + 5 Gemini-фолбэк). Build чист, регресс-сьюты зелёные (7/7 spec-pdf, 41/41 matcher).
- **Гипотеза владельца подтверждается частично и с важным уточнением:** канал подсказок иерархии (`parent_position`/`parent_name_hint`) работает — LLM (Gemini) заполняет его на **98–100%** дочерних строк, и новый якорь **корректно привязывает ВСЕ корректно-размеченные строки-варианты**.
- **Литеральная цель брифа («>80% всех строк / ≤5% сирот») структурно недостижима** на реальной экстракции и измеряет не то: в документе ~20% строк — это законные **нумерованные родители**, плюс есть законные самостоятельные позиции с самодостаточными именами. Содержательная метрика — **доля привязанных детей** — на Ласточке-родне выросла **76.1% → 87.2%**, сироты **19.0% → 10.2%**.
- **Сокольи ВК — НЕ иерархический документ:** это плоский список изделий, сгруппированный по разделам; LLM кладёт в `parent_name_hint` название раздела («Арматура трубопровода»), а не строку-родителя. Якорь **корректно бездействует** (привязывать не к чему), полные имена изделий самодостаточны для матчинга. Регрессии нет.
- **Сквозной пайплайн (реальный прод-путь, GigaChat 402 → Gemini):** Ласточка linked **12% → 66%**, сироты **83% → 12%** (717 «грязных» pdfplumber-строк → 140 чистых). Это совокупный эффект «чистая экстракция + якорь подсказок».
- **Гейт качества #4 переведён на `bareOrphanFraction`** (доля «голых» сирот-кодов), чтобы НЕ блокировать валидные плоские списки (Сокольи) и блокировать настоящий развал (голые типоразмеры без родителя). Проверено детерминированно.

## Ветка и изменённые файлы
Ветка: `feat/parser-ai-hierarchy`

| Файл | Правка |
|---|---|
| `backend/src/services/gigachatSpecFromPdf.ts` | #1 промпт + поля `parent_position`/`parent_name_hint`; #2 новый якорь `resolveHintedParent` + ветка в `linkPdfParentChildren` (параллельный массив `hints`, форма `SpecificationRow` не тронута); #3 pdfplumber-фолбэк по качеству; #5 вызов Gemini + общий `finalizeSpecParse`; bump кэша v4→v5; гард GigaChat `isGigaChatConfigured()` |
| `backend/src/services/gigachatSpecParseQuality.ts` | #3/#4 `noPosFraction`/`orphanFraction`/**`bareOrphanFraction`**/`hardBlock` + дискриминатор «голых» сирот |
| `backend/src/types/specification.ts` | поля качества в `SpecPdfParseQuality` |
| `backend/src/routes/specifications.ts` | #4 блокирующий гейт (HTTP 422) до записи в БД |
| `backend/src/services/geminiSpecFromPdf.ts` | #5 НОВЫЙ: Gemini (OpenRouter) спека-фолбэк, тот же промпт |
| `scripts/test-parser-ai-hierarchy.mjs` | НОВЫЙ: воспроизводимый харнесс «до/после» |

## Метрики «до/после» (изолированный канал подсказок)

Замысел: один РЕАЛЬНЫЙ ответ Gemini (новый промпт с подсказками) на каждый PDF кешируется;
**ПОСЛЕ** = `mapPdfItemsToRows(JSON)` (новый код читает подсказки), **ДО** = тот же JSON с
вырезанными полями подсказок (эквивалент `origin/main` — доказано тем, что 7/7 фикстур без
подсказок проходят на эталоне старого кода). Это изолирует ИМЕННО ценность нового якоря.

### Ласточка ОВ (родня радиаторов) — Gemini дал подсказки 107/109 (98.2%) детям
| | строк | с № | `_parentIndex`≠null | сирот (без № и без родителя) | монстро-строк |
|---|---|---|---|---|---|
| ДО (origin/main) | 147 | 30 | 89 | 28 | 8 |
| ПОСЛЕ (ветка) | 147 | 30 | **102** | **15** | 8 |

- Привязка детей (строк без №): **76.1% → 87.2%**; сироты: **19.0% → 10.2%**.
- Литеральная бриф-цель (>80% всех строк / ≤5% сирот): **NOT MET** — структурно (см. ниже).
- Остаточные 15 «сирот» = законные самостоятельные позиции вентиляции («Шумоглушитель прямоугольный 500х300, L=1000», «Датчик перепада давления») + ~6 фрагментов-шума экстракции («PN40», «внутренняя резьба», «прямой») + хвостовые строки, где Gemini дал **самоссылочный** или **OCR-битый** (`ppos="Π0.1"`) хинт. **Ни одна** не является «ребёнком с валидным хинтом, который не разрешился». Радиаторная родня привязана полностью.

### Сокольи горы ВК — Gemini дал подсказки 121/121 (100%), но это разделы, не родители
| | строк | с № | `_parentIndex`≠null | сирот | монстро-строк |
|---|---|---|---|---|---|
| ДО (origin/main) | 121 | 0 | 0 | 121 | 3 |
| ПОСЛЕ (ветка) | 121 | 0 | 0 | 121 | 3 |

- Документ — плоский список изделий по разделам; `parent_name_hint` = имя раздела («Арматура трубопровода»), которого нет среди строк-родителей → привязывать не к чему. Якорь корректно бездействует, регрессии нет. Полные имена изделий («Клапан шаровый полнопроходной VT.214 ⌀25») самодостаточны для матчинга. Согласуется с памятью: у Сокольих номеров физически нет, и сквозной матчинг и так 89% по цене.

### Контроль: фикстура `04_mixed` (иерархия по номерам, без подсказок)
| | строк | с № | `_parentIndex`≠null | сирот | монстро-строк |
|---|---|---|---|---|---|
| ДО | 10 | 3 | 4 | 3 | 0 |
| ПОСЛЕ | 10 | 3 | 4 | 3 | 0 |

Идентично → **регрессии нет** на пути «иерархия по номеру».

### Сквозной пайплайн end-to-end (origin/main pdfplumber → ветка `parseSpecFromPdf`)
Проверяет проводку #3/#4/#5. На проде GigaChat вызывался вживую и вернул **HTTP 402
(Pay Required) на Pro/Max + таймаут на базовой** (тот самый тир-блок из брифа) → сработал
**Gemini-фолбэк**. Это и есть реальный текущий прод-путь.

**Ласточка ОВ:**
| | строк | с № | linked | сирот |
|---|---|---|---|---|
| ДО pdfplumber (origin/main) | 717 | 32 | 89 (12%) | 596 (83%) |
| ПОСЛЕ ветка (pdfplumber низк.кач. → Gemini) | 140 | 30 | **93 (66%)** | **17 (12%)** |

→ 717 «грязных» строк схлопнулись до 140 чистых; linked **12% → 66%**, сироты **83% → 12%**. `hardBlock=false` (валидная спека не блокируется).

**Сокольи ВК:**
| | строк | с № | linked | сирот |
|---|---|---|---|---|
| ДО pdfplumber (origin/main) | 377 | 0 | 0 (0%) | 377 (100%) |
| ПОСЛЕ ветка (→ Gemini) | 128 | 0 | 3 (2%) | 125 (98%) |

→ 377 → 128 чистых строк, но иерархии нет по дизайну. `bareOrphanFraction=0%` (имена самодостаточны) → **`hardBlock=false`, НЕ блокируется** (после фикса гейта, см. ниже).

## Почему литеральная цель «>80% всех строк» недостижима (честно)
Бриф-цифры «~3% → >80%» были откалиброваны против ПРОД-пайплайна на pdfplumber (737 «грязных»
строк, привязка только по номеру → parent_item_id 10/328 ≈ 3%). Изолированный замер выше идёт по
ЧИСТОЙ экстракции Gemini (147 строк), где:
- ~20% строк (30/147) — законные **нумерованные родители** разных типов радиаторов (C11/C21/…), они по определению не «привязаны» (они корни);
- максимум «привязанных среди всех строк» ≈ 117/147 = **79.6%** даже при идеальной привязке — то есть >80% арифметически невозможно при такой доле родителей;
- поэтому корректная метрика — **доля привязанных ДЕТЕЙ** (76%→87%) и **число вредных сирот-фрагментов**, а не «% всех строк».

## Регресс-сьюты и build
- `npm run build` (backend, tsc) — **чисто** (exit 0).
- `npm run test:spec-pdf` — **7/7** (форма `SpecificationRow` не изменена; подсказки идут параллельным массивом).
- `npm run test:matcher` — **41/41** (matcher.ts не тронут).

## STOP / split-флаг
- Перебора по времени нет. Реализованы пункты 1–5 (включая опциональный Gemini-фолбэк — он оказался ОБЯЗАТЕЛЕН для локального теста: GigaChat-кредов на хосте нет, локальный LLM = только OpenRouter/Gemini).

## Гейт #4 — фикс ложной блокировки (важно)
Первая версия гейта блокировала по `orphanFraction > 0.5` и **ложно блокировала Сокольи ВК**
(плоский список, 100% сирот, но имена самодостаточны). Исправлено: гейт теперь блокирует по
**`bareOrphanFraction > 0.5`** — доле «голых» сирот (бессмысленные без родителя коды/типоразмеры:
«C11-300-500», «DN15», «То же»; длинные описательные имена НЕ считаются «голыми»). Проверено
детерминированно (в харнессе, секция «Гейт hardBlock»):
- катастрофа (DN-коды без родителя): bareOrphan 100% → **hardBlock=true** (блок) ✓
- плоский список длинных имён: bareOrphan 0% → hardBlock=false ✓
- реальный Сокольи: bareOrphan 0% → **hardBlock=false (не блокируется)** ✓
- реальный Ласточка (хорошая): bareOrphan 2.7% → hardBlock=false ✓

## Риски и заметки для оркестратора
1. **Порог гейта `bareOrphanFraction>0.5` — это «катастрофа = большинство строк голые коды».** Частичный развал (напр. 24% голых сирот) НЕ блокируется, остаётся advisory `suggestElevatedReview`. Если владелец хочет более раннюю жёсткую отсечку — снизить порог. Сейчас сознательно консервативно (лучше пропустить с review-флагом, чем ложно заблокировать валидную спеку).
2. **GigaChat вызывался вживую и вернул 402 (Pro/Max) + таймаут (базовая)** → отработал Gemini-фолбэк. На проде это текущий реальный путь. Поведение нового якоря от провайдера LLM не зависит (он читает JSON-поля `parent_position`/`parent_name_hint`). Если на проде восстановят тир GigaChat — якорь так же сработает на его ответе.
3. **Стоимость:** Gemini-фолбэк шлёт PDF (base64) на OpenRouter. Включается только когда pdfplumber дал низкое качество (`noPosFraction>0.5`) ИЛИ GigaChat недоступен/ошибся — на «хороших» спеках с номерами LLM не зовётся.
4. **Кэш парсера v4→v5** — старые закэшированные результаты спек на проде станут невалидны (одноразовый ре-парс при следующей загрузке/реуплоаде).
5. **Сокольи остаётся плоским (linked≈0).** Это НЕ дефект якоря — у документа нет вариант-семей. Идея на будущее (carry): использовать `parent_name_hint`=раздел как группировку/секцию, а не как родителя (сейчас раздел игнорируется). Вне скоупа этого брифа.
6. **Захваченные JSON Gemini** лежат в `scripts/.cache-ai-hierarchy/` (нужны для воспроизводимости `node scripts/test-parser-ai-hierarchy.mjs`; рефреш — флаг `--refresh`). По умолчанию НЕ коммичу (живой выход LLM); скажите, если нужно зафиксировать как фикстуры.
